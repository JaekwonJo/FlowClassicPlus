# CODEX 연속작업 인수인계 문서

- 문서명: `CODEX_연속작업_인수인계_20260304.md`
- 최신 업데이트: 2026-03-31 17:51 KST
- 최신 업데이트: 2026-03-31 18:34 KST
- 최신 업데이트: 2026-04-01 10:35 KST
- 최신 업데이트: 2026-04-01 10:49 KST
- 최신 업데이트: 2026-04-01 11:02 KST
- 최신 업데이트: 2026-04-01 11:18 KST
- 최신 업데이트: 2026-04-01 11:34 KST
- 최신 업데이트: 2026-04-02 05:25 KST
- 최신 업데이트: 2026-04-02 06:00 KST
- 최신 업데이트: 2026-04-02 06:08 KST
- 최신 업데이트: 2026-04-02 13:14 KST
- 최신 업데이트: 2026-04-02 13:21 KST
- 최신 업데이트: 2026-04-03 14:05 KST
- 최신 업데이트: 2026-04-03 14:32 KST
- 최신 업데이트: 2026-04-03 15:02 KST
- 최신 업데이트: 2026-04-03 15:18 KST
- 최신 업데이트: 2026-04-03 15:29 KST
- 최신 업데이트: 2026-04-03 15:44 KST
- 최신 업데이트: 2026-04-03 16:08 KST
- 최신 업데이트: 2026-04-03 06:05 KST
- 최신 업데이트: 2026-04-03 16:42 KST
- 최신 업데이트: 2026-04-04 00:22 KST
- 최신 업데이트: 2026-04-04 09:18 KST
- 최신 업데이트: 2026-04-04 09:34 KST
- 최신 업데이트: 2026-04-04 09:44 KST
- 최신 업데이트: 2026-04-04 10:02 KST
- 최신 업데이트: 2026-04-04 10:11 KST
- 최신 업데이트: 2026-04-04 10:18 KST
- 최신 업데이트: 2026-04-04 10:27 KST
- 최신 업데이트: 2026-04-04 10:35 KST
- 최신 업데이트: 2026-04-06 11:05 KST
- 최신 업데이트: 2026-04-06 11:18 KST
- 최신 업데이트: 2026-04-06 11:24 KST
- 최신 업데이트: 2026-04-06 11:31 KST
- 최신 업데이트: 2026-04-06 11:41 KST
- 최신 업데이트: 2026-04-06 11:47 KST
- 최신 업데이트: 2026-04-06 11:55 KST
- 최신 업데이트: 2026-04-06 12:01 KST
- 최신 업데이트: 2026-04-06 12:13 KST
- 최신 업데이트: 2026-04-10 13:40 KST
- 최신 업데이트: 2026-04-13 04:45 KST
- 최신 업데이트: 2026-04-13 04:58 KST
- 최신 업데이트: 2026-04-13 05:09 KST
- 최신 업데이트: 2026-04-13 05:15 KST
- 최신 업데이트: 2026-04-14 00:07 KST
- 최신 업데이트: 2026-04-14 00:17 KST
- 최신 업데이트: 2026-04-19 12:04 KST
- 최신 업데이트: 2026-04-19 12:28 KST
- 최신 업데이트: 2026-04-19 13:01 KST
- 목적: 다음 Codex가 지금 상태를 빠르게 이해하고 이어서 작업할 수 있게 하는 문서

## 1. 현재 안정 상태
- 원본 클래식은 건드리지 않고 보존
- 실제 개발판은 `Flow Classic Plus`
- 현재 기준 고정판: `2026년 3월 12일 완성품`
- GitHub `main` 최신 안정 커밋: `b725581`
- Git 태그 기준점: `complete-2026-03-12`
- 새 세션 시작용 파일: `새 Codex 시작용 한방 프롬프트.txt`
- `flow/flow_config.json`은 각 컴퓨터별 로컬 설정 파일이므로 절대 GitHub 최신본으로 덮어쓰지 않음. 코드 pull 시에도 config는 보존/병합이 원칙
- 사용자 확인 완료 기능:
  - 프롬프트 자동화 정상
  - S001~S### 자동 반복 정상
  - 다운로드 자동화 정상
  - 프롬프트 생성 옵션 자동찾기/테스트 정상
  - S 생성 옵션 자동찾기/테스트 정상
  - 이어달리기 `프롬프트 -> S자동화 -> 다운로드` 정상
  - 데스크탑/노트북 모두 media 전환 테스트 정상

## 2. 최근 사용자 확인 완료 사항
- 스토리 프롬프트 파이프라인 로그/창 상태 자동 저장 + 화살표 복귀 감지:
  - 2026-04-19 기준, `ttz_worker`는 창을 크게 늘리거나 `로그 숨기기`를 바꿔도 다음 실행 때 다시 기본 크기/기본 로그 상태로 돌아가 사용자가 매번 다시 맞춰야 했음
  - `story_pipeline/core.py`에 `log_visible` 설정값을 추가했고, `story_pipeline/app.py`는 시작 직후 최신 설정 형식으로 한 번 저장하면서 창 크기와 로그 표시 상태를 worker 설정 파일에 자동 반영하게 수정. 다시 켜도 마지막 창 크기와 `로그 보기/숨기기` 상태가 그대로 복원됨
  - 응답 대기 로직도 `story_pipeline/web.py`에서 본문 멈춤만 보지 않고, Gemini 하단 버튼이 `네모(중지)`에서 `화살표(전송 가능)`로 돌아왔는지 먼저 확인하게 보강. 화살표 복귀가 보이면 짧게 한 번 더 확인한 뒤 바로 다음 단계로 넘어가고, 버튼 감지가 안 맞는 경우에만 기존 본문 안정화 대기로 폴백함
- 스토리 프롬프트 파이프라인 입력 지연/HUD 보강:
  - 2026-04-19 기준, `ttz_worker`는 브라우저 창이 막 뜨는 순간 바로 긴 프롬프트를 입력해 앞부분이 잘리는 문제가 있었음
  - 이제 `story_pipeline/web.py`에 `pre_input_delay_seconds`를 추가해 입력 전 `창 뜬 뒤 기다림` 카운트다운을 먼저 수행하고, 긴 프롬프트는 `human_typing_enabled`가 켜져 있어도 자동으로 `전체 붙여넣기`로 전환하게 수정
  - `story_pipeline/app.py`에는 상단 HUD를 추가해 `현재 단계 / 지금 하는 일 / 배치 진행 / 묶음 진행 / 장면 진행 / 남은 기다림 / 지난 시간`을 실시간 표시하고, 작업 설정 문구도 `시작 장면`, `끝 장면`, `한번 보낼 장면 수`, `창 뜬 뒤 기다림`, `보낸 뒤 첫 확인`처럼 쉬운 말로 바꿔 둠
- 스토리 프롬프트 파이프라인 2차 UI 축소 + TrustedHTML 오류 수정:
  - 2026-04-19 기준, 1차 HUD는 정보가 너무 쪼개져 있고 창이 커져서 사용자가 보기 복잡했음
  - `story_pipeline/app.py`는 이제 창 기본 크기를 더 작게 잡고, 상단 HUD를 `상태 / 핵심 한 줄 / 진행 요약 / 남은 기다림 / 경과시간`만 남긴 작은 카드로 다시 정리. `Step 파일 / 장면 파일 / Gem URL / 결과 폴더`는 긴 파일명 표시를 없애고 버튼만 남겼고, 설정도 `시작 / 끝 / 한번 보낼 장면 / 창 뜬 뒤 기다림 / 보낸 뒤 첫 확인 / 새 채팅 / 메모장 저장` 정도만 보이게 축소
  - `story_pipeline/web.py`의 전체 붙여넣기 로직은 `innerHTML`을 쓰지 않고 자식 노드를 지운 뒤 텍스트 노드를 넣는 방식으로 바꿔 Gemini의 `TrustedHTML` 제한 오류를 피하게 수정
  - 자동화 실패 시에는 `runner.close()`로 브라우저가 즉시 닫혀 원인 확인이 어려웠으므로, 이제 오류가 난 경우 브라우저를 남겨 두고 로그에 직접 닫아 달라는 안내를 남김
- 스토리 프롬프트 파이프라인 실행 버튼 줄 복구:
  - 2026-04-19 기준, 작은 창에 맞추며 `브라우저 / 시작 / 중지 / 폴더` 버튼이 오른쪽 좁은 패널에 밀려 실제로 안 보이는 상태가 되었음
  - 지금은 버튼을 별도 `실행` 카드로 분리해 항상 보이게 다시 배치했고, 사용자가 물어본 `보낸 뒤 첫 확인` 용어도 `보낸 뒤 잠깐 기다림`으로 바꿔 의미를 더 쉽게 맞춤
- 스토리 프롬프트 파이프라인 실행기 VBS 보정:
  - `6_스토리_프롬프트_파이프라인_실행.vbs`, `7_스토리_프롬프트_워커_추가실행.vbs`는 한글 문자열/따옴표 조합 때문에 Windows Script Host 컴파일 오류가 날 수 있어, 메시지 문자열을 ASCII 안전형으로 바꾸고 `.bat`는 `start "" wscript.exe ...` 방식으로 실행하게 수정
- 이미지 워커 연결 프롬프트 시작 번호 인식 보강:
  - 2026-04-14 기준, 이미지 워커의 일반 프롬프트 파서는 `S045 Prompt`는 읽지만 `S045>S046 Prompt` 같은 연결 장면 문법은 일반 번호로 못 읽어 파일 안 순서대로 1번, 3번처럼 잘못 잡는 문제가 있었음
  - 지금은 `_parse_prompt_source_entries()`에도 연결 장면 해석을 넣어 `S045>S046 Prompt`는 시작 번호 `45`, `S049>S050 Prompt`는 시작 번호 `49`로 읽고 `source_tag`도 `S045`, `S049`로 맞추게 수정
- 프롬프트 파일 번호 요약 표시 보강:
  - 2026-04-14 기준, 워커 기본 설정의 프롬프트 파일 정보는 `최소번호~최대번호`로만 보여서 `045~080`처럼 중간 빈 번호가 모두 있는 것처럼 보였음
  - 지금은 프롬프트 파일 안 실제 `source_no`만 뽑아 `045,046,049,050,080`처럼 번호 목록으로 직접 표시. 워커 기본 설정 한 줄과 프롬프트 선택 팝업 요약이 같은 형식을 함께 사용
- 자동화스튜디오 누락 결과 접두어 표시 보강:
  - 2026-04-13 기준, 파일관리에서 `@S###` 파일도 스캔은 되지만 누락 결과 문자열은 항상 `S053,S078`처럼 `S` 접두어로만 나와, 실제 폴더 파일명(`@S053`)과 결과가 안 맞았음
  - 지금은 폴더 안에 보이는 대표 파일명 형식을 먼저 보고, `@S###` 폴더면 누락도 `@S053,@S078`처럼 맞춰 출력하고 일반 `S###` 폴더면 기존처럼 `S053,S078`로 출력
- 자동화스튜디오 파일관리 `@S###` 스캔 보강:
  - 2026-04-13 기준, `⚡자동화스튜디오.html`의 파일관리에서 누락 확인은 `S001` / `_001` 쪽만 주로 잡아 `@S001.png`, `@S021 (2).mp4` 같은 실제 실사용 파일명을 번호로 인식하지 못했음
  - 지금은 파일명 번호 추출을 공용 함수로 묶어 `@S### / S### / _001 / _S001`을 모두 같은 장면 번호로 읽게 수정. 누락 확인은 이 공용 함수를 쓰고, 이미 `@S###` 또는 `S###` 형태인 파일은 이름변경 대기 목록에 다시 안 뜨게 정리
- inline 레퍼런스 `@S999가` 연속 문자 처리 보강:
  - 2026-04-13 기준, 이미지 워커/S자동화 워커의 inline 레퍼런스 감지는 `@(S?\d{3,4})\b` 정규식이라 `@S999가`, `@S999는`처럼 한글 조사나 영문이 공백 없이 붙으면 태그 경계로 인식하지 못했음
  - 지금은 `@(S?\d{3,4})(?!\d)`로 바꿔 `@S999` 뒤에 공백이 없어도 숫자 태그만 먼저 끊어 첨부하고, 뒤의 한글/영문 문장은 일반 본문으로 이어서 입력되게 수정
- 자동화스튜디오 예외 프롬프트 파서 정리 + 바탕화면 복사본 제거:
  - 2026-04-13 기준, `004 ||| 내용` 같은 예외 조건이 `|||` 기준으로 먼저 잘려 `004`와 본문이 분리되면서 실제 결과에 반영되지 않는 문제가 있었음. 추가로 `007>008 ||| 내용` 같은 연결 장면 문법도 결과 생성기에 반영되지 않았음
  - 이제 실사용 원본 `C:\Users\jaekw\autostudio\⚡자동화스튜디오.html`은 예외 조건을 `한 줄/블록 전체`로 먼저 읽어 `004 ||| 내용`, `S004 Prompt : 내용`, `007>008 ||| 내용`, `S007>S008 Prompt : 내용`을 모두 인식함. 연결 장면은 결과에 `S007>S008 prompt : ...` 줄이 중간에 따로 추가되게 수정
  - 바탕화면의 `⚡자동화스튜디오.html` 복사본은 삭제했고, 대신 `⚡자동화스튜디오.url` 바로가기만 남김. 다음 Codex도 바탕화면 html 복사본을 다시 만들지 말고, `autostudio` 원본만 수정해야 함
- 프롬프트 파일 레지스트리 추가 + 누락 슬롯 자동 복구:
  - 2026-04-08 기준, `on_add_slot()`이 새 `flow_prompts_slot_*.txt` 파일을 만들었어도 `prompt_slots` 목록 저장이 어딘가에서 빠지면, 워커를 다시 열 때 목록에는 없고 실제 파일만 남는 경우가 반복됨. 이 상태에서 다음 슬롯 추가를 하면 같은 번호 파일을 다시 덮어쓸 위험도 있었음
  - 이제 `flow_prompt_slots_registry.json`에 슬롯 목록과 삭제된 슬롯 파일 목록을 별도로 같이 저장하고, 앱 시작 시 디스크의 `flow_prompts_slot_*.txt` 파일 중 목록에만 누락된 항목은 자동 복구하도록 보강. 새 슬롯 생성 시에도 config 목록뿐 아니라 실제 파일 존재 여부까지 같이 확인해 이미 있는 slot 파일을 덮어쓰지 않게 수정
- 프롬프트 파일/프로젝트 목록 저장 덮어쓰기 방지 + 저장된 브라우저 프로필 유지:
  - 2026-04-07 기준, 워커에서 프롬프트 파일/프로젝트를 추가한 뒤 저장해도 메인 앱이 예전 메모리 상태를 다시 `flow_config.json`에 저장하면서 최신 목록을 덮어써, 워커를 껐다 켜면 추가한 슬롯/프로젝트가 다시 사라지는 경우가 있었음
  - 이제 메인 config 저장 전 현재 프로세스가 공용 목록을 직접 바꾸지 않았다면 디스크의 최신 `prompt_slots` / `project_profiles`를 우선 보존하게 바꿔, 워커가 만든 최신 목록이 메인 앱 저장 때문에 사라지지 않도록 보강
  - 추가로 워커 재실행 시 `_launch_worker_process()`가 기존 worker config 안의 `browser_profile_dir`를 먼저 유지하게 바꿔, 워커 안에서 `새 프로필`로 바꾼 브라우저 프로필이 다음 실행 때 다시 워커명 기본값으로 덮어쓰이는 문제를 수정
- 워커 수동 저장 버튼 + 프로젝트/프롬프트 파일 자동저장 보강:
  - 2026-04-07 기준, 이미지 워커/S자동화 워커에서 프로젝트를 추가하거나 바꾸고 창을 닫으면 다음 실행 때 목록/선택이 다시 예전 상태처럼 보이는 경우가 있었고, 이미지 워커는 프롬프트 파일 추가 후에도 마지막 선택이 안정적으로 남지 않는 문제가 있었음
  - 이제 이미지 워커/S자동화 워커 하단 실행줄에 `💾 저장` 버튼을 추가했고, 프로젝트 선택 변경/프로젝트 추가·이름변경·URL편집·삭제는 즉시 자동 저장되게 조정. 이미지 워커의 프롬프트 파일 콤보 선택과 `추가/이름수정/삭제`도 작업 직후 자동 저장되어, 워커를 껐다 켜도 마지막 상태가 더 잘 유지되게 보강
- 워커 대기열 3열 확대 + 실패 번호 즉시 복사:
  - 2026-04-06 기준, 이미지 워커/S자동화 워커 대기열은 넓은 창에서도 2열로 머무는 경우가 많아 한 화면에 보는 정보량이 적었고, 실패 번호 복붙은 완료 요약 파일이나 logs로 들어가야 해서 즉시 재시도하기 불편했음
  - 이제 대기열 3열 전환 기준 폭을 낮춰 넓은 창에서 더 빨리 3칸으로 배치되게 바꿨고, 헤더에 `실패 번호 복붙` 버튼을 추가해 현재 대기열에서 `실패` 상태인 번호만 압축 형식으로 바로 클립보드에 복사할 수 있게 보강
- 워커 대기열 `지우기` 시 상단 진행 상황도 같이 초기화:
  - 2026-04-07 기준, 워커 하단 대기열에서 `지우기`를 눌러도 카드 목록만 사라지고 상단 진행 상황/HUD는 이전 세션의 `10 / 11`, `100 / 100` 같은 값이 그대로 남아 새 작업 전 헷갈리는 문제가 있었음
  - 이제 `지우기`는 완료된 세션의 대기열, 진행률, 선택 요약, 세션 경로, HUD 상태를 함께 초기화해 `0 / 0`과 `준비 완료`로 바로 복귀. 작업 중에는 실수 방지를 위해 `지우기`를 막고 안내 메시지를 띄움
- S자동화 워커 정렬 텍스트 폴백 정밀화:
  - 2026-04-06 기준, 프레임 탐색과 검색창 재활성화 보강 뒤에도 정렬 버튼을 못 찾는 로그가 반복되어, 단순 `get_by_text('최신순')` 1개 반환 방식으로는 현재 UI 위치를 안정적으로 못 집는 문제 확인
  - 이제 텍스트 폴백은 `최신순/오래된 순/최근 사용` 등 후보를 전부 모아 검색창 오른쪽 같은 줄에 있는 항목만 점수화해서 가장 가까운 텍스트를 정렬 버튼 후보로 사용
- S자동화 워커 정렬 적용 순서 보강:
  - 2026-04-06 기준, 제공된 스크린샷상 정렬 드롭다운은 검색창이 열린 오버레이 안에서 보이는데, 로그에서는 검색창을 찾자마자 곧바로 정렬 버튼 탐색을 시도해 아직 오버레이가 덜 활성화된 타이밍에 실패할 수 있었음
  - 이제 S워커 정렬 적용 전에는 검색창을 먼저 클릭해 오버레이를 활성화하고, 첫 시도에서 정렬 버튼을 못 찾으면 검색창을 다시 활성화한 뒤 한 번 더 정렬 탐색을 재시도하도록 보강
- S자동화 워커 정렬 버튼 프레임 탐색 추가:
  - 2026-04-06 기준, 정렬 버튼 탐색 강화 후에도 실사용 로그에서 여전히 `S에셋 정렬 버튼을 찾지 못해 기본 정렬로 계속합니다.`가 반복되어, 정렬 UI가 메인 문서가 아닌 오버레이/프레임 쪽 구조에 떠 있을 가능성 확인
  - `_resolve_prompt_reference_sort_button()`는 이제 `self.page`만 보지 않고 `page.frames` 전체의 `button / role=button / role=combobox / aria-haspopup / aria-expanded` 후보를 같이 스캔하도록 보강
- S자동화 워커 정렬 버튼 탐색 강화:
  - 2026-04-06 기준, 실사용 로그에서 `S에셋 정렬 버튼을 찾지 못해 기본 정렬로 계속합니다.`가 먼저 찍혀, 최신순 항목 선택 로직보다 앞단의 정렬 버튼 탐색 자체가 실패하는 경우 확인
  - 이제 정렬 버튼 탐색은 `button`, `role=button` 외에 `role=combobox`, `aria-haspopup='menu'`, `aria-expanded` 후보까지 같이 보고, 그래도 못 찾으면 `최신순/오래된 순` 텍스트 locator를 검색창 오른쪽 근처에서 잡는 폴백을 추가
- S자동화 워커 `최신 순` 대상 항목 재정의:
  - 2026-04-06 기준, 드롭다운 안에 `최신순`이 두 개 보이는 화면에서는 단순히 가장 아래 최신순을 고르기보다, 사용자가 원하는 것은 `오래된 순` 바로 위에 있는 `최신순` 항목이었음
  - 이제 `최신 순` 선택 시에는 메뉴 안에서 `오래된 순` 위치를 먼저 찾고, 그 바로 위쪽에 있는 `최신순` 항목을 우선 클릭하도록 보강
- S자동화 워커 `최신 순` 강제 재선택 보강:
  - 2026-04-06 기준, Flow 정렬 버튼은 현재값에도 `최신순` 문구가 보여 첫 보정 뒤에도 코드가 `이미 최신순`이라고 오판하고 메뉴를 생략할 수 있었음
  - 이제 `최신 순`일 때는 버튼 라벨만 보고 통과하지 않고, 반드시 드롭다운을 열어 아래쪽 실제 `최신순` 메뉴 항목을 다시 선택하도록 보강
- S자동화 워커 `최신 순` 메뉴 중복 보정:
  - 2026-04-06 기준, Flow 에셋 정렬 드롭다운에는 `최신순` 문구가 위쪽 현재값 근처와 실제 메뉴 항목 쪽에 중복으로 보일 수 있어, 첫 구현은 위쪽 항목을 집어 최신 생성순 정렬이 아닌 잘못된 위치를 누를 수 있었음
  - 이제 `최신 순` 선택 시에는 정렬 버튼 bbox 아래쪽에 뜨는 후보들만 우선 보고, 그중 가장 아래에 있는 `최신순` 메뉴 항목을 우선 클릭하도록 보강
- S자동화 워커 에셋 정렬 순서 선택 추가:
  - 2026-04-06 기준, S자동화는 최신 이미지 에셋을 계속 참조해야 할 때가 많은데 기존 코드는 에셋 검색창 정렬을 사용자가 수동으로 맞춘 상태에 의존하거나, inline 레퍼런스 쪽 `오래된 순` 강제만 따로 있어 S워커에서 `최신 순`으로 고정 작업할 수 없었음
  - 이제 S자동화 워커 번호 설정 아래에 `에셋 정렬 순서` 드롭다운(`오래된 순 / 최신 순`)을 추가했고, 선택값은 `asset_search_sort_order`로 저장되며 워커를 닫았다 다시 열어도 유지됨
  - S자동화 실행 중 첫 에셋 검색 때만 현재 선택한 정렬을 1회 적용하고, 그 뒤에는 같은 세션에서 다시 정렬 메뉴를 건드리지 않고 계속 그 순서로 에셋을 찾도록 보강
- S자동화 개별 프롬프트 파일 단순 형식 추가:
  - 2026-04-09 기준, 기존 S자동화 개별 프롬프트 파일은 `S001::...`, `S001 Prompt : ...` 같은 형식만 주로 떠올려야 해서 사용자가 메모장 작성 규칙을 매번 기억하기 번거로웠음
  - 이제 `001 : 본문 |||`처럼 번호와 본문만 쓰는 간단한 형식도 지원하며, `001 :` 다음 줄부터 여러 줄 본문을 쓰고 마지막을 `|||`로 닫는 방식도 함께 허용
  - 실행 시에는 내부에서 자동으로 `S001 Prompt : 본문` 형태로 완성해 입력하므로, 파일에는 번호와 본문만 적으면 됨. 기존 `S001::...`, `S001 Prompt : ...`, `S001`+다음 블록 본문 형식은 그대로 유지
- S자동화 최종 프롬프트 접두어 정규화:
  - 2026-04-09 기준, S자동화 개별 프롬프트 파일을 태그형/번호형/공통 fallback으로 섞어 쓸 때, 최종 실행 직전 조립 단계에서 공통 프롬프트나 템플릿 fallback은 `S001 Prompt :` 접두어 없이 본문만 입력되는 문제 확인
  - 원인은 `_build_asset_loop_items()`가 태그형 프롬프트는 그대로 쓰고, 태그가 없는 공통 fallback이나 템플릿도 본문만 그대로 넘기던 점
  - 이제 S자동화 실행 직전에는 프롬프트 출처와 무관하게 현재 번호 기준으로 한 번 더 정규화해서, `S001::...`, `001 : ...`, `S001 : ...`, 공통 fallback, 템플릿 fallback 모두 `S001 Prompt : ...` 형태로 맞춘 뒤 입력하도록 보강
- 워커 프로젝트 목록 재실행 덮어쓰기 방지:
  - 2026-04-04 기준, 워커 안에서 프로젝트를 추가/이름변경/URL편집하면 워커 cfg와 메인 `flow_config.json` 파일에는 저장되지만, 이미 떠 있는 메인창은 메모리에 예전 `project_profiles`를 들고 있어서 다음 워커 실행 때 그 오래된 목록으로 워커 cfg를 다시 덮어써 변경이 사라지는 문제 확인
  - 원인은 `_launch_worker_process()`가 새 워커를 띄울 때 `self.cfg["project_profiles"]`만 기준으로 워커 cfg를 다시 맞추던 점
  - 이제 런처는 워커 실행 직전에 디스크의 메인 `flow_config.json`을 다시 읽어 최신 `project_profiles`와 `prompt_slots`를 가져오고, 그 최신 목록으로 워커 cfg를 보정한 뒤 실행하도록 변경
- 워커 종료 시 마지막 값 저장:
  - 2026-04-04 기준, 이미지 워커/S자동화 워커에서 프로젝트, 번호 범위, 저장 폴더 등을 바꾼 뒤 `시작`을 누르지 않고 그냥 창을 닫으면 일부 값이 다음 실행 때 유지되지 않는 불편 확인
  - 원인은 종료 루트가 `_persist_ui_options() -> on_option_toggle()`만 타고, 미니 워커 전용 `worker_*_var` 값을 cfg로 옮기는 `_apply_prompt_worker_compact_to_cfg / _apply_asset_worker_compact_to_cfg / _apply_download_worker_compact_to_cfg`는 실행 버튼/작업봇 버튼 때만 호출되던 점
  - 이제 `_persist_ui_options()` 시작에서 `_persist_worker_compact_options()`를 먼저 호출해, 워커 종료 직전 현재 미니 워커 UI 값을 cfg에 강제로 반영하고 공용 프로젝트/프롬프트 목록도 같이 동기화한 뒤 저장하도록 보강
- 제출 버튼 상단 오탐 차단:
  - 2026-04-04 기준, inline 레퍼런스 프롬프트를 제출할 때 입력창 하단 오른쪽 화살표 대신, 상단 에셋 창/상단바에 떠 있는 `생성` 버튼을 제출 버튼으로 잘못 집어 에셋 창을 다시 여는 사례 확인
  - `_resolve_submit_locator_for_input()`는 이제 geometry를 먼저 쓰고, selector 탐색도 입력창 bbox 기준 `같은 행 ±80px`, `입력창 오른쪽 절반 이후`, `입력창 오른쪽 +120px 이내` 조건을 통과한 버튼만 허용
  - meta에 `애셋/에셋/asset/검색/search/오래된/정렬/업로드`가 들어간 상단 버튼은 제출 후보에서 제외해, 상단 오버레이가 떠 있어도 실제 하단 제출 화살표를 우선 집도록 정리
- 프롬프트 제출 버튼 우선 보강:
  - 2026-04-04 기준, inline 레퍼런스가 여러 개인 프롬프트에서 입력은 끝났는데 실제 제출은 안 되었고, 로그상 `제출 검증 완료: Enter=OK`로 잘못 통과해 다운로드 단계로 넘어가는 사례 재확인
  - 원인은 `_confirm_submission_started()`가 사유 문자열까지 함께 반환하도록 바꾼 뒤 호출부가 이를 분리하지 않으면 튜플 자체를 참으로 볼 수 있는 위험이 있었고, 동시에 inline 레퍼런스 뒤에는 포커스가 애매해 `Enter`보다 실제 제출 버튼 클릭이 더 안정적일 가능성이 컸던 점
  - 지금은 호출부를 `submitted, reason = ...` 형태로 바로잡고, inline 레퍼런스 프롬프트는 `제출 버튼 클릭 우선 -> 엄격 검증`, 일반 프롬프트는 `Enter -> 엄격 검증 -> 버튼 클릭 폴백` 순서로 바꿨음
  - 성공 판정도 `submit_disabled / submit_changed / generation_indicator / input_cleared` 같은 더 강한 신호로만 인정하고, action log에 `Enter=FAIL(timeout)` / `Button=OK(submit_changed)` 같은 식으로 사유까지 남게 정리
- 워커 번호 제안 PID 추적 보강:
  - 2026-04-04 기준, 런처 쪽 슬롯 예약 보강 뒤에도 일부 환경에서는 `_worker_runtime` 파일 인식이 흔들려, 같은 메인창에서 이미 이미지 워커가 떠 있어도 다음 런처가 다시 `이미지_워커1`을 제안하는 경우 확인
  - 이제 메인창은 자기가 `subprocess.Popen()`으로 직접 띄운 워커 프로세스 객체를 `kind -> index -> proc` 형태로 메모리에서도 함께 추적하고, 번호 제안 시 살아 있는 `proc.poll() is None` 항목과 `_worker_runtime/*.json` 파일을 둘 다 합쳐 사용 중 번호를 계산
  - 그래서 같은 메인창에서 연속으로 열 때는 슬롯 파일 인식이 잠깐 흔들려도 `1 사용 중 -> 2`, `1/2 사용 중 -> 3`, 닫히면 다시 빈 번호로 돌아가는 흐름이 더 안정적으로 유지됨
- 완료 요약 저장 폴더 사본 보강:
  - 2026-04-04 기준, 작업 종료 후 `completion_summary_*.txt`는 `logs` 폴더에만 남아 사용자가 실제 이미지/S영상 저장 폴더에서는 결과 요약을 바로 찾기 불편하다는 요청 확인
  - `_save_completion_summary()`에서 기존 `logs` 저장은 유지하면서, payload의 `output_dir`와 이어달리기 단계별 `entries[].output_dir`를 모아 중복 없이 각 저장 폴더에도 같은 요약 파일을 한 번씩 같이 저장하도록 보강
  - 완료 팝업에도 첫 번째 `저장 폴더 사본` 경로를 같이 보여, 사용자가 `logs`를 다시 열지 않고도 바로 결과 폴더에서 요약 txt를 찾을 수 있게 정리
- 워커 번호 제안 슬롯 예약 보강:
  - 2026-04-04 기준, 메인창에서 이미지 워커/S자동화 워커를 다시 열 때 이미 `워커1`이 떠 있어도 새 런처 제안이 계속 `워커1`로만 나오는 문제 확인
  - 원인은 번호 제안이 `_worker_runtime` 슬롯 파일 존재 여부를 기준으로 보는데, 실제 슬롯 파일은 새 워커 프로세스가 충분히 뜬 뒤에야 남아서 런처 쪽에서는 빈 폴더로 보일 수 있던 점
  - 이제 메인 런처가 `subprocess.Popen()` 직후 자식 PID로 슬롯 파일을 먼저 예약하고, 워커가 바로 죽으면 그 예약 파일도 바로 지우도록 보강
  - 그래서 이미 `1`이 살아 있으면 다음은 `2`, `2`도 살아 있으면 `3`, `1`이 꺼졌으면 다시 `1`이 제안되는 흐름으로 복구
- 워커 상단 상태 라벨 잘림 보강:
  - 2026-04-04 기준, 워커 상단 우측 `현재 상태`는 `생성 대기 후 다운로드... S016 | 13초`처럼 긴 문구가 들어오면 오른쪽에서 일부 잘려 보이는 문제 확인
  - 이제 워커 모드에서는 상태 라벨을 조금 더 좁은 굵기 폰트로 바꾸고, 긴 문구는 `태그/남은초` 기준으로 줄바꿈 또는 축약해 표시하게 조정
- S자동화 워커 실패 시 이미지 다운로드 폴백:
  - 2026-04-04 기준, S자동화 워커를 단독으로 돌릴 때 영상 다운로드가 실패하면 바로 실패로 끝나, 같은 화면에 남아 있을 수 있는 이미지 결과를 활용하지 못하는 불편이 있었음
  - 번호 설정 영역에 `실패 시 이미지 다운로드하기` 체크를 추가했고, 이 옵션을 켜면 영상 다운로드 실패 직후 같은 S번호로 이미지 다운로드를 1회 다시 시도
  - 이미지 폴백이 성공하면 전체 항목은 성공으로 처리되고, 영상과 이미지가 둘 다 실패할 때만 최종 실패 처리
- S자동화 에셋 검색창/영상 준비 보강:
  - 2026-04-03 기준, 최신 Flow 화면에서 S자동화가 처음 진입할 때는 `현재 생성 모드 버튼을 찾지 못했습니다. (asset/image)`로 한 번 실패했다가, 곧바로 같은 화면에서 재시도하면 `image -> video` 전환과 `S002` 에셋 검색 입력이 정상 진행되는 흔들림 로그 확인
  - 원인은 `S자동화 시작 전 video 보장`과 `에셋 selector 자동탐색/테스트`가 실제 실행 때 쓰는 폴백보다 약해서, 첫 렌더 타이밍이나 `애셋 검색` 표기/일반 input 형태를 놓치기 쉬운 점
  - 이제 S자동화 시작 전 media 상태 감지와 `image→video` 전환은 짧은 재시도를 넣어 첫 진입 흔들림을 줄였고, 에셋 검색 입력창 탐지는 `애셋/에셋` 표기, 일반 `input/textarea/contenteditable/searchbox`, 포커스/상단 오버레이 형태까지 같이 점수화하는 공용 탐색기로 통합
  - 자동탐색/테스트/실행이 모두 같은 에셋 검색창 탐색 함수를 쓰도록 맞춰, 최신 UI에서도 저장 selector 갱신과 실제 S자동화 입력 흐름이 더 비슷하게 움직이도록 보강
- 워커 대기열 2~3열 + 마우스 휠 스크롤 보강:
  - 2026-04-03 기준, 통합 워커 하단 대기열은 카드 높이를 줄였어도 여전히 한 줄에 1개씩만 보여 넓은 화면 오른쪽이 비고, 마우스 휠로도 스크롤이 잘 안 되는 불편이 있었음
  - 이제 대기열 캔버스가 자기 자신을 스크롤 대상으로 직접 등록하고, 화면 폭에 따라 `1열/2열/3열`로 자동 배치되게 변경
  - 이미지 워커와 S자동화 워커는 같은 공용 `_build_worker_queue_panel / _refresh_worker_queue_panel`을 쓰므로 둘 다 동시에 적용됨
- 워커 하단 `완전정지 / 일시정지 / 재개` 버튼 추가:
  - 2026-04-03 기준, 메인 작업창에는 pause/resume/stop이 있었지만 미니 워커 하단 실행줄에는 `중지`만 있어, 워커 중심 사용 흐름에서 즉시 제어가 불편했음
  - 이제 이미지 워커 / S자동화 워커 / 다운로드 워커 하단 실행줄에 모두 `완전정지`, `일시정지`, `재개` 버튼을 추가하고, 메인 버튼 상태와 같이 움직이게 동기화
- 휴식 후 자동 재개 보강:
  - 기존에는 바이오 브레이크가 끝난 뒤에도 finally 구간에서 일반 작업 간격을 한 번 더 기다려, 사용자 체감상 `휴식 끝났는데 안 깨어난 것처럼` 보일 수 있었음
  - 이제 휴식 종료 직후 `t_next = now + 1초`로 잡아 다음 작업을 바로 이어서 준비하게 수정
  - 추가로 통합 워커의 `생성 후 다운로드 대기` 루프는 `paused` 상태에서 더 이상 실패 처리하지 않고, 재개할 때까지 멈춰 있다가 그대로 이어지게 보강
- 통합 다운로드 혼합 결과 처리 보강:
  - 2026-04-03 기준, `S010`처럼 같은 검색 화면 안에 성공 카드와 실패 카드가 같이 섞여 있을 때, 직전 보강에서는 실패 카드가 보인다는 이유만으로 다운로드 시도 자체를 멈춰 버려 실제로는 받을 수 있는 정상 결과도 놓치는 문제 확인
  - 이제 다운로드 함수는 `결과 없음(empty)`만 즉시 실패로 보고, `실패 카드(failure)`는 화면에 보이더라도 현재 검색창 값이 요청 번호와 맞고 타일이 실제로 남아 있으면 같은 검색 화면 안에서 성공 카드의 `... -> 다운로드` 탐색을 계속 시도
  - 반대로 성공 타일을 끝내 못 찾았고 실패 카드만 남은 경우에는 마지막에 그 실패 사유로 종료하므로, 다른 번호의 카드(S003/S006 등)를 메인 화면에서 주워 받는 일은 계속 막힘
- 워커 대기열 카드 높이 축소:
  - 2026-04-03 기준, 통합 워커 하단 `이미지 생성+다운로드 대기열` / `S생성+다운로드 대기열`은 각 항목 카드와 진행 막대가 너무 두꺼워 스크롤을 자주 내려야 하는 불편이 있었음
  - 이제 행 간격, 상하 패딩, 폰트 크기, 막대 높이를 줄여 한 화면에 더 많은 대기열 항목이 보이도록 압축
- 통합 다운로드 실패 카드 오다운로드 방지:
  - 2026-04-03 기준, `프롬프트자동화+다운로드` / `S자동화+다운로드`에서 `S004`, `S007`처럼 검색 결과가 실제로는 실패 카드이거나 `선택한 항목과 일치하는 결과가 없습니다` 상태인데도, 다운로드 로직이 `보이는 타일이 있으면 맨 왼쪽 위 카드로 폴백`하는 길을 타서 이전 S003/S006 같은 주변 카드를 받아버리는 문제 확인
  - 원인은 다운로드 함수가 `tile_count > 0`만으로 대표 카드 폴백을 허용하고, 검색 실패/정책 위반/문제 발생 카드가 한번 보였는지도 별도로 막지 않던 점
  - 이제 다운로드 검색 화면에서 `결과 없음`, `문제가 발생했습니다`, `정책 위반`, `Google 정책` 같은 실패 상태를 즉시 감지해 바로 실패 처리하고, 단순히 타일이 보인다는 이유만으로 다른 카드의 더보기 버튼을 누르지 않도록 가드 추가
- 통합 워커 화질 드롭다운 추가:
  - 2026-04-03 기준, `프롬프트자동화+다운로드`와 `S자동화+다운로드`는 저장 폴더는 워커 안으로 들어왔지만, 다운로드 화질은 여전히 메인 작업창의 공용 `download_image_quality` / `download_video_quality`를 따라가야 해서 워커만으로 마무리하기 불편했음
  - 이제 이미지 워커에는 `이미지 화질(4K/2K/1K)` 드롭다운, S자동화 워커에는 `영상 화질(1080P/720P/4K)` 드롭다운을 추가했고, 워커에서 고른 값이 해당 워커 설정 파일과 메인 표시 변수에 같이 반영되게 보강
  - 통합 워커 요약줄에도 현재 화질이 같이 표시되므로 저장 폴더와 함께 다운로드 조건을 워커 안에서 바로 확인 가능
- 통합 워커 저장 폴더 UI 추가:
  - 2026-04-03 기준, `프롬프트자동화+다운로드` 이미지 워커와 `S자동화+다운로드` 워커는 실제로 다운로드를 수행하는데도 워커 안에서는 저장 폴더를 직접 고를 수 없고, 메인 작업창/다운로드 워커의 공용 `download_output_dir`에 의존하던 상태였음
  - 이제 이미지 워커와 S자동화 워커에도 각각 `저장 폴더` 입력칸과 `폴더 선택` 버튼을 추가했고, 각 워커 설정 파일의 `download_output_dir`에 바로 저장되게 보강
  - 통합 워커 요약줄에도 현재 저장 폴더가 같이 보이게 바꿔서, 어느 워커가 어느 폴더로 내려받는지 워커 안에서 바로 확인 가능
- 워커 프로젝트 URL 즉시 반영 보강:
  - 2026-04-03 기준, 워커에서 프로젝트를 바꿔도 `작업봇 창 열기` 직전 `on_option_toggle()`가 숨겨진 예전 `start_url` 입력값을 다시 cfg에 덮어써, 현재 워커에서 선택한 프로젝트 주소가 아니라 이전 주소로 열리는 문제 확인
  - 이제 워커 모드에서는 `start_url` 저장 시 일반 숨김 입력칸보다 `현재 워커에서 선택한 프로젝트`를 우선으로 다시 동기화하고, `작업봇 창 열기`와 `새로고침` 직전에도 같은 URL 동기화를 한 번 더 수행하도록 보강
  - 추가로 워커 `URL 편집`은 기존 짧은 `askstring` 대신 넓은 전용 입력창으로 바꿔 긴 Flow 프로젝트 주소도 한 번에 확인/수정하기 쉽게 조정
- 워커 공용 목록 즉시 동기화 보강:
  - 2026-04-03 기준, 이미지 워커에서 `프롬프트 파일 추가`를 누르면 새 `flow_prompts_slot_XX.txt` 파일은 만들어졌지만, 곧바로 메인 공용 설정을 다시 읽어오면서 방금 추가한 슬롯 목록이 사라지는 문제 확인
  - 원인은 워커가 슬롯/프로젝트 수정 내용을 자기 설정 파일에만 저장하고, 공용 목록 원본인 `flow_config.json`에는 같이 반영하지 않던 구조였음
  - 이제 워커에서 `프롬프트 파일 추가/이름수정/삭제`와 `프로젝트 추가/이름수정/URL편집/삭제`를 하면 메인 공용 설정에도 같은 목록을 즉시 같이 써주도록 보강해서, 워커 안에서 만든 목록이 바로 유지되게 수정
- 통합 워커 2단계 랜덤 대기 추가:
  - 2026-04-03 기준, 이번에 합친 `프롬프트자동화+다운로드`, `S자동화+다운로드`는 기존처럼 대기시간 1개만 쓰면 `생성 대기`와 `다음 작업 대기` 성격이 섞여 제어가 불편했음
  - 이제 통합 워커에는 대기시간이 2개로 분리됨. `생성 후 다운로드 대기`는 설정값 기준 `+랜덤만` 적용해서 너무 일찍 다운로드를 시도하지 않게 하고, `다운로드 성공 후 다음 작업 대기`는 설정값 기준 `±랜덤`으로 흔들리게 조정
  - 현재 랜덤 폭은 둘 다 기준값의 약 25%이며, 예를 들어 `40초`면 1차 대기는 `40~50초`, 2차 대기는 `30~50초` 범위로 동작
  - 이 규칙은 이미지 워커와 S자동화 워커의 통합 다운로드 흐름에만 적용되고, 단독 다운로드 워커는 기존 방식 그대로 유지
- 다운로드 다중 결과 카드 더보기 선택 보강:
  - 2026-04-02 기준, 다운로드 자동화에서 검색 결과가 1장일 때는 큰 대표 카드의 `...`을 잘 눌렀지만, 2~4장처럼 여러 카드가 보이면 카드 안에 `S010` 같은 태그가 직접 안 보여 `검색 결과에 항목이 없습니다` 또는 `더보기 버튼을 찾지 못했습니다` 쪽으로 흔들리던 문제 확인
  - 다운로드 카드 선택 로직을 `첫 카드 1개만 보기`에서 `보이는 카드들을 점수화해 대표 카드 고르기`로 바꾸고, 카드 안에 태그가 안 보여도 페이지 자체가 해당 S번호 검색 결과로 확인되면 그 대표 카드 기준으로 `...`을 계속 찾도록 보강
  - 추가로 사용자가 원하는 기준에 맞춰, 검색 결과 타일이 여러 장 보이면 이제 태그 문구가 카드 안에 보이지 않아도 `맨 왼쪽 위 카드=최신 카드`로 보고 그 카드의 `...` 버튼을 먼저 찾도록 단순화
- 작업 간격 기준점 수정:
  - 2026-04-02 기준, 메인 작업창이나 워커에서 `작업 간격 40초`로 넣어도 다음 실행 타이머를 작업 시작 직후 먼저 잡고 있어, 실제 작업이 30초 걸리면 완료 후 10초만 쉬고 바로 다음 작업으로 넘어가던 문제 확인
  - 이제 다음 타이머는 현재 작업이 끝난 뒤(`_run_task / _run_download_task finally`)에 다시 잡도록 바꿔, 사용자가 넣은 작업 간격이 `완료 후 대기` 기준으로 그대로 체감되게 수정
- Autoupload 런타임 경로 정리:
  - 2026-04-02 기준, 현재 `Flow Classic Plus` 폴더에서 메인창을 띄워도 `%LOCALAPPDATA%\Autoupload\runtime\python-embed\python311._pth` 안에 예전 `C:\Users\jaekw\Documents\autoupload` 경로가 남아 있으면 워커 import가 옛 코드로 새어 `_tkinter.TclError: unknown option "-padding"` 같은 과거 오류가 다시 뜨는 문제 확인
  - `0_원터치_설치+실행.bat`는 이제 `FlowClassicPlus\runtime`를 우선 쓰고, 필요하면 구 `Autoupload\runtime`을 새 위치로 옮기거나 임시 fallback 하도록 조정. `Flow_Start.vbs / run_silent.vbs`와 기존 Autoupload 호환 배치도 모두 Classic Plus 기준 진입점으로 연결하고, 현재 구 런타임의 `.pth`에서도 `autoupload` 경로를 즉시 제거하도록 정리
- 다운로드 워커 상단 빠른 시작/HUD 동기화 수정:
  - 2026-04-02 기준, 다운로드 워커에서 아래 미니 패널로 프로젝트를 바꾸고 `연속 1~119` 같은 범위를 넣어도, 위쪽 HUD의 `🤖 작업봇 창 열기` / `▶ 다운로드 시작` 버튼은 그 값을 cfg에 먼저 저장하지 않고 예전 프로젝트 URL과 번호 범위로 바로 실행하던 문제 확인
  - 상단 빠른 버튼도 이제 다운로드 워커 미니 패널의 적용 로직을 먼저 타도록 바꿔, 새로 고른 프로젝트 주소/번호/저장폴더/모드가 그대로 반영된 뒤 시작되게 수정. HUD 프로젝트 표시도 현재 워커 선택값과 즉시 같이 보이도록 보강
- 워커 실행 경로/초기화 순서 보강:
  - 2026-04-01 기준, 워커 실행 실패 팝업에 예전 `CRASH_LOG.txt`의 `unknown option "-padding"` 문구가 섞여 보여 실제 원인 추적을 방해하던 문제 확인
  - 워커 프로세스 시작 전에 기존 `CRASH_LOG.txt`를 비우고, 현재 `Flow Classic Plus` 폴더를 `sys.path` 맨 앞에 넣는 bootstrap 방식으로 실행하도록 보강해 예전 `Documents/autoupload` 코드가 섞일 가능성을 줄임
  - 추가로 워커 HUD 총개수 미리보기 로직이 너무 일찍 실행돼 `progress_var` 초기화 전 충돌하던 문제를 막아, 워커가 시작 직후 바로 종료되지 않도록 보정
- 워커 상단 HUD 총개수 미리보기 보정:
  - 2026-04-01 기준, S자동화/다운로드 워커에서 `연속 130~136`처럼 범위를 바꿔도 상단 HUD/진행상황은 예전에 계산된 총개수(예: 9, 119)를 그대로 보여 헷갈리던 문제 확인
  - 워커 패널의 `연속/개별/프롬프트 슬롯` 값이 바뀔 때마다 실제 실행 예정 번호 개수를 다시 계산해, 시작 전 상단 HUD가 바로 `0 / 7`처럼 현재 선택 기준으로 보이도록 보강
- 이미지 워커 시작 시 숨은 본문값 덮어쓰기 차단:
  - 2026-04-01 기준, 이미지 워커에서 `연속 130~136`처럼 미니 워커 패널에서 방금 넣은 값으로 시작해도, 공통 시작 로직이 숨겨진 기존 본문 입력칸 값을 다시 `on_option_toggle()`로 덮어써 예전 `1-119` 같은 값 검사를 타면서 `프롬프트 개별 실행 번호가 비어 있거나 범위를 벗어났습니다.` 경고가 나던 문제 확인
  - 워커 모드(`prompt/asset/download`)에서 시작 버튼을 누를 때는 더 이상 숨은 본문 UI 값을 다시 읽지 않고, 워커 패널에서 방금 저장한 cfg를 그대로 사용하도록 조정
- 이미지 워커 목록 새로고침 추가:
  - 2026-04-01 기준, 이미지 워커는 처음 열릴 때 프롬프트 파일 슬롯 목록을 한 번만 읽어서, 메인 작업창에서 새 슬롯/파일을 추가해도 워커 쪽 콤보박스에 바로 반영되지 않던 문제 확인
  - 워커 상단에 `목록 새로고침` 버튼을 추가해 메인 `flow_config.json`의 최신 `project_profiles / prompt_slots`를 다시 읽고, 이미지 워커의 프로젝트/프롬프트 파일 목록을 즉시 갱신하도록 보강
- 워커 실행 `list index out of range` 복구:
  - 2026-04-01 기준, 메인 설정의 프로젝트 목록이 늘어난 뒤 예전에 저장된 `flow_config_이미지_워커1.json` 같은 워커 설정 파일이 더 적은 프로젝트만 들고 있으면, 워커 실행 직전 부모 프로세스에서 오래된 프로젝트 인덱스를 그대로 집어 `list index out of range`가 나던 문제 확인
  - 워커 실행 직전에 `project_profiles`를 메인 설정 기준으로 다시 맞추고, `active_project_profile`도 실제 워커 설정 파일 안의 프로젝트 개수에 맞게 안전하게 잘라서 이미지/S자동화/다운로드 워커가 다시 정상 실행되도록 복구
- `봇 출동 준비!` 팝업 ON/OFF 추가:
  - 2026-03-31 기준, 메인 작업창 옵션에 `봇 출동 준비 팝업` 체크를 추가
  - 이 체크를 끄면 우측 하단 카운트다운 팝업이 더 이상 뜨지 않고, 이미 떠 있는 팝업도 바로 닫히도록 연결
- 워커 번호 추천 규칙 단순화:
  - 2026-03-31 기준, 워커 이름 기본값은 더 이상 기존 설정 파일/프로필 폴더 개수를 보고 계속 올라가지 않음
  - 프로그램을 새로 켜면 다시 `이미지_워커1 / S자동화_워커1 / 다운로드_워커1`부터 시작하고, 같은 실행 안에서 실제로 워커를 연 경우에만 2, 3으로 증가하도록 조정
- 워커 실행 방식 수정:
  - 2026-03-31 기준, 워커 새 창 실행은 이제 메인 앱과 같은 `-m flow.flow_auto_v2` 방식으로 호출하도록 수정
  - 기존처럼 파일 직접 실행으로 열면 패키지 import 경로가 꼬여 워커가 바로 꺼질 수 있었고, 이 경우 런처 숫자만 계속 증가하던 문제를 줄이기 위해 즉시 종료 시 실패 팝업도 같이 띄우도록 보강
- 워커 창/봇 창 겹침 완화:
  - 2026-03-31 기준, 여러 워커를 연속으로 띄울 때 메인 워커 창이 전부 화면 중앙에 겹쳐 뜨던 부분을 워커 번호 기준 배치로 보강
  - 같은 번호 규칙을 브라우저 `작업봇 창`에도 같이 적용해서, 이미지/S자동화/다운로드 워커가 각자 크롬 창을 조금씩 다른 위치에 띄우도록 조정
- 워커 HUD 식별 정보 보강:
  - 2026-03-31 기준, 워커 상단 HUD는 이제 `상태`뿐 아니라 현재 `설정 파일 / 브라우저 프로필`도 같이 표시
  - 여러 워커를 동시에 띄웠을 때 어떤 창이 어떤 설정/프로필을 쓰는지 헤더 바로 아래에서 즉시 구분하기 쉽게 보강
- 워커 HUD 바로 실행 버튼 추가:
  - 2026-03-31 기준, 실제 워커 창 상단 HUD에 `🤖 작업봇 창 열기`와 역할별 `바로 시작` 버튼 추가
  - 이미지 워커는 `▶ 이미지 시작`, S자동화 워커는 `▶ S자동화 시작`, 다운로드 워커는 `▶ 다운로드 시작`으로 바로 이어질 수 있게 연결
- 워커 HUD 배치 교정:
  - 2026-03-31 기준, 워커 상단 HUD는 이제 본문 패널보다 먼저 배치되게 수정
  - 실제 워커 창에서 헤더 바로 아래에 `워커명 / 프로젝트 / 상태 / 진행률`이 먼저 보이도록 배치 교정
- 워커 런처 하단 문구 제거 + 워커 상단 HUD 추가:
  - 2026-03-31 기준, 워커 런처 아래의 `실행 요청 완료 | 다음: ...` 상태 문구는 이제 아예 숨김
  - 대신 실제 워커 창 상단에 `워커명 / 프로젝트 / 상태 / 진행률`만 보이는 작은 HUD를 추가해, 런처보다 작업창에서 바로 현재 상황을 보도록 정리
- 워커 런처 상태 문구 간소화:
  - 2026-03-31 기준, 워커 런처 아래 상태는 길게 설명하지 않고 `실행 요청 완료 | 다음: 워커이름`처럼 현재 요청과 다음 추천 이름만 짧게 보여주도록 정리
- 다운로드 워커 런처 2차 확장:
  - 2026-03-31 기준, `다운로드 워커 실행` 창에서도 이제 `다운로드 모드(이미지/동영상) / 품질 / 작업 간격 / 번호 방식(연속/개별) / 저장 폴더`까지 실행 전에 바로 정할 수 있게 보강
  - 다운로드 워커는 번호 선택을 기존 다운로드 자동화와 같은 `연속 범위/개별 번호` 방식으로 쓰고, 저장 폴더도 런처에서 바로 고를 수 있게 조정
- 워커 실행 팝업 단순화:
  - 2026-03-31 기준, `워커 실행 확인 필요` 경고는 제거하고 런처 하단 상태 문구만 `실행 요청 완료`로 갱신하게 정리
  - 사용자는 같은 런처를 계속 띄워둔 채 워커2, 워커3를 연속해서 추가 생성하는 흐름에 더 집중할 수 있게 조정
- 워커 런처 추가 생성 흐름 보강:
  - 2026-03-31 기준, 워커 런처는 이제 `grab_set` 모달을 제거해 메인창/새 워커 창을 같이 만질 수 있게 조정
  - `이미지 워커 열기 / S자동화 워커 열기` 버튼을 눌러도 런처가 바로 닫히지 않고, 새 워커 프로세스를 띄운 뒤 같은 런처 안에서 다음 워커 이름으로 계속 추가 생성할 수 있게 보강
  - S자동화 워커 런처는 높이와 폭을 키우고 개별 번호 입력칸도 더 넓혀, 하단이 잘리거나 긴 번호를 붙여넣기 불편하던 문제를 줄이도록 조정
- 워커별 색 구분 추가:
  - 2026-03-31 기준, 워커 창은 이름 끝번호(예: 이미지_워커1, 이미지_워커2)를 기준으로 헤더/강조색을 다르게 적용
  - 같은 종류 워커를 여러 개 동시에 띄워도 파란/청록/초록/주황/보라처럼 색으로 더 빠르게 구분할 수 있게 보강
- S자동화 워커 런처 2차 확장:
  - 2026-03-31 기준, `S자동화 워커 실행` 창에서 이제 `생성 개수(x1~x4) / 작업 간격 / 번호 방식(연속/개별) / S개별 프롬프트 파일 사용`까지 실행 전에 바로 정할 수 있게 보강
  - 연속 범위는 `시작~끝`으로 저장하고, 개별 번호는 기존 `S001,S005,S021-S030` 또는 `1,5,21-30` 형식을 그대로 사용할 수 있게 연결
- 이미지 워커 런처 2차 확장:
  - 2026-03-31 기준, `이미지 워커 실행` 창에서 이제 `프로젝트 / 프롬프트 파일 / 생성 개수(x1~x4) / 작업 간격 / 번호 방식(전체/연속/개별)`을 실행 전에 바로 정할 수 있게 보강
  - 연속 범위는 내부적으로 프롬프트 개별 실행 스펙(`12-48`)으로 변환해 저장하고, 개별 번호는 기존 `1,5,9-12` 형식을 그대로 씀
  - 이미지 워커는 앞으로 메인창에서 누를 때마다 사실상 “가벼운 프롬프트 워커 런처”처럼 쓰는 방향으로 계속 다듬는 중
- 공통 설정 파일 분리 시작:
  - 2026-03-31 기준, 휴식/주기적 새로고침은 워커별 config와 별도로 `flow_shared_settings.json` 공통 파일에 같이 저장되도록 기반 추가
  - 목표는 메인 작업창에서 공통값을 바꾸면 이미지/S자동화/다운로드 워커가 같은 값을 읽고, `작업 간격`만 워커별 설정 파일에 따로 남게 만드는 것
- 워커 런처 사용성 보강:
  - 2026-03-31 기준, 워커 런처 높이와 하단 여백을 늘려 버튼/안내문이 잘리지 않게 조정
  - 같은 역할 카드를 다시 눌렀을 때는 기본 이름을 `이미지_워커2`, `이미지_워커3`처럼 자동 증가 추천하도록 보강해, 워커 여러 개를 빠르게 추가로 띄우기 쉽게 조정
- 워커 모드 2차 UI 경량화:
  - 2026-03-31 기준, `--worker`로 열린 워커 창은 상단 제목/부제가 역할별로 바뀌고(`이미지 워커`, `S자동화 워커`, `다운로드 워커`)
  - 자기와 관계없는 자동화 섹션(프롬프트/S자동화/다운로드)과 시작 버튼은 숨기도록 1차 경량화 적용
  - 워커 우측 상단의 `이어달리기` 버튼은 숨기고, `메인 메뉴` 버튼은 `워커 닫기`로 바꿔 일반 작업창과 역할을 더 쉽게 구분하도록 조정
- 병렬 워커 1차 기반 시작:
  - 2026-03-31 기준, 현재 `main` 안정판은 유지하고 별도 브랜치 `feature/worker-windows`에서 병렬 워커 기능 개발 시작
  - 메인창 `프롬프트 자동화 / S001 자동화 / 다운로드 자동화` 카드는 이제 기존 작업창 이동 대신 각 역할용 워커 런처를 열도록 1차 기반 추가
  - 워커 런처는 `워커 이름 / 설정 파일 이름 / 브라우저 프로필 이름 / 프로젝트 선택`을 받고, 같은 프로그램을 새 프로세스로 다시 띄우되 `--worker --config --target` 인자로 워커 역할별 시작 위치를 분리
- 브라우저 프로필/설정 파일 이름 분리 기반 추가:
  - 브라우저 프로필은 이제 `새 브라우저 프로필 만들기`뿐 아니라 `프로필 이름 변경` 버튼으로 직접 이름을 바꿀 수 있음
  - 설정 파일도 `설정 파일 저장하기 / 설정 파일 이름 변경` 버튼으로 `flow_config_이름.json` 형태의 별도 파일로 저장 가능하게 보강
  - 목표는 이미지 워커1 / S자동화 워커1 / 다운로드 워커1처럼 프로필과 설정 파일 이름을 같이 맞춰 두고 병렬 실행 시 헷갈리지 않게 만드는 것
- S개별 프롬프트 `태그 블록 + 본문 블록` 형식 지원:
  - 2026-03-31 기준, S개별 프롬프트 파일은 기존 `S032::본문`뿐 아니라 `S032 ||| 실제 본문` 형태도 같이 지원
  - 그래서 실제 입력창에 `S032 Prompt : ...` 전체를 넣고 싶을 때 `S032 ||| S032 Prompt : ...`처럼 저장해도 번호는 `S032`로 읽고 본문은 그대로 입력하도록 보강
- 이어달리기 S단계별 개별 프롬프트 파일 지원:
  - 2026-03-31 기준, 이어달리기 `작업 상세 설정`에서 S반복 자동화 단계마다 `S개별 프롬프트 파일 사용` 체크와 파일 선택을 따로 저장할 수 있게 추가
  - 그래서 같은 이어달리기 안에 S자동화 단계를 2개/3개 넣어도 각 단계가 서로 다른 개별 프롬프트 파일을 적용하도록 보강
- 실시간 실패 오탐지 축소:
  - 2026-03-31 기준, 실패 감지는 이제 `실패` 제목만 있다고 바로 기록하지 않음
  - 같은 카드 안에 `문제가 발생했습니다`, `Google 정책을 위반할 수 있습니다` 같은 실제 실패 본문이 같이 있을 때만 실패로 인정하도록 좁혀서 `warning / undo / 프롬프트 재사용 / 삭제` 버튼 텍스트 오탐지를 줄이도록 조정
- 실시간 실패 카드 카드-서명 추적 보강:
  - 2026-03-31 기준, 실패 감지는 이제 단순히 화면에 보이는 실패 카드 개수 증가만 보지 않음
  - 실패 문구가 포함된 텍스트 요소에서 실제 결과 카드 컨테이너까지 다시 찾아 올라간 뒤, 카드 위치+요약문 서명 변화를 기준으로 새 실패를 추적하도록 보강
  - 그래서 회색 카드가 같은 자리에서 천천히 실패 카드로 바뀌는 경우를 더 잘 따라가도록 조정
- 이어달리기 실패 패널 누적 유지:
  - 2026-03-31 기준, 이어달리기에서 S자동화 -> 다운로드처럼 다음 단계로 넘어가도 우측 `실시간 실패 번호` 패널은 같은 실행 안에서 초기화되지 않음
  - 이전 단계 실패를 계속 누적해서 보이게 유지해, 중간에 실패가 사라진 것처럼 보이던 혼란을 줄이도록 조정
- 실시간 실패 감시 x1/x2 대응:
  - 2026-03-31 기준, 실패 감시는 이제 프롬프트/S자동화의 현재 생성 개수(`x1~x4`)를 읽어 제출 1건당 감시 슬롯도 그 개수만큼 큐에 등록
  - 그래서 사용자가 `x2`로 생성 중일 때 회색 카드 2개가 따로 늦게 실패로 바뀌어도, 단일 watch 하나로 덮어써져 놓치던 문제를 줄이도록 조정
- 실시간 실패 감시 큐 전환:
  - 2026-03-31 기준, 실패 감시는 더 이상 최근 제출 1개만 보는 단일 watch 구조가 아님
  - 회색 카드가 늦게 실패로 바뀌는 경우를 잡기 위해, 최근 제출들을 큐로 보관하고 새 실패 카드가 보이면 가장 오래된 미해결 번호부터 순서대로 매핑하도록 변경
  - 다음 작업 시작 직전에도 실패 카드 감지를 한 번 더 시도해, 대기 끝 무렵에 늦게 뜨는 실패도 최대한 놓치지 않게 조정
- 실시간 실패 카드 탐색 범위 확대:
  - 2026-03-31 기준, Flow 실패 카드는 클릭형 타일일 수 있어 기존 `div/section/article/li`만 보는 방식으로는 놓칠 수 있었음
  - 이제 실패 감지는 `button`, `a`, `role=button/link`까지 같이 훑고, 새 key 비교만 아니라 실패 카드 개수 증가/요약문 반복 증가도 함께 보도록 완화
- S자동화 에셋 없음 후 `start_t` 예외 수정:
  - 에셋 없음 분기 안에서 세션 로그 duration을 계산할 때 `start_t`가 아직 만들어지기 전이라 `cannot access local variable 'start_t'` 예외가 날 수 있었음
  - 이 2차 예외 때문에 `self.index += 1` 이후 흐름이 깨져 같은 번호(S031)를 다시 집는 현상이 생겼고, 이제 `start_t`를 작업 시작 직후 미리 만들어서 에셋 없음 처리 후 바로 다음 번호로 진행되게 수정
- S자동화 에셋 없음 후 강제 초기화:
  - `선택한 항목과 일치하는 결과가 없습니다`가 뜬 경우, 단순 `page.reload()`로는 현재 상세 화면/포커스가 남아 다음 번호가 프롬프트 입력창에 잘못 들어갈 수 있었음
  - 이제는 에셋 없음 감지 후 해당 번호를 실패 처리한 다음 `start_url`로 강하게 돌아가고, `asset_video_ready_for_run`도 다시 초기화해서 다음 번호는 깨끗한 화면에서 다시 시작
- 에셋 검색 포커스 폴백 안전화:
  - Step2에서 에셋 검색 입력칸을 못 찾았을 때는 이제 `asset/search/에셋/검색` 메타가 확인된 포커스 입력칸에서만 포커스 폴백을 허용
  - 검색 메타가 없는 포커스 입력칸에는 더 이상 S번호를 직접 타이핑하지 않고, DOM 검색 입력칸 탐색이 실패하면 즉시 오류로 처리
- S자동화 에셋 없음 즉시 실패 처리:
  - 2026-03-30 기준, S자동화에서 에셋 검색 결과에 `일치하는 결과 없음`이 뜨면 더 이상 그 번호에 프롬프트를 입력하지 않음
  - 이제는 해당 번호를 즉시 실패 처리하고 `실시간 실패 번호` 박스/리포트에 같이 남긴 뒤, 짧게 대기하고 새로고침 후 다음 번호로 넘어가게 조정
- 실시간 실패 감시 루프 보강:
  - 2026-03-30 기준, 프롬프트/S자동화의 Flow 실패 감시는 더 이상 `대기 중(t_next)`일 때만 잠깐 도는 구조가 아님
  - 이제 `pending_generation_watch`가 살아 있는 동안은 `_tick()`마다 계속 실패 카드 감지를 시도하도록 바꿔, 실패 박스가 비어 있던 문제를 줄이도록 조정
- 하단 카운트다운 안내창 이동 가능:
  - 2026-03-30 기준, `⚡ 봇 출동 준비!` 하단 카운트다운 팝업은 화면을 덜 가리도록 투명도를 조금 낮추고, 팝업 본문을 마우스로 드래그해 원하는 위치로 이동할 수 있게 조정
- 주기적 새로고침 시점 조정:
  - 2026-03-30 기준, 프롬프트 자동화/S자동화의 `주기적 새로고침`은 더 이상 제출 직후 바로 실행하지 않음
  - 이제는 제출 후 대기 구간에서 Flow 실패 카드 감시를 계속 유지하고, 지정 개수에 도달했을 때는 `다음 작업 시작 직전`에만 새로고침 + 랜덤 안정화 대기를 수행하도록 변경
  - 그래서 실패 카드가 뜨기도 전에 새로고침돼 `실시간 실패 번호` 박스에 누락되던 문제가 줄어듦
- 프롬프트/S자동화 실시간 실패 번호 박스 추가:
  - 2026-03-30 기준, 제출 후 Flow 카드에 `실패 / 문제가 발생했습니다`가 새로 뜨면 오른쪽 패널 `실시간 실패 번호` 박스에 해당 번호를 즉시 누적 표시하도록 보강
  - 이때 감지된 번호는 종료 후 `session_report`와 `completion_summary`의 실패 번호 복붙에도 같이 반영되며, 박스 상단 `복사` 버튼으로 작업 중에도 바로 복사 가능
- 이어달리기 프롬프트 개별 실행 체크 연동:
  - 2026-03-30 기준, 이어달리기에서 `프롬프트 자동화` 단계를 `개별 번호` 또는 범위 문자열로 적용할 때는 이제 작업창의 `개별 실행 사용` 체크도 함께 켜지게 보강
  - 그래서 이어달리기 시작 시 프롬프트 단계가 번호 문자열만 채워 넣고 체크는 꺼진 채 남아 전체 실행으로 보이던 문제가 사라짐
- S자동화 첫 항목만 video 확인:
  - 같은 S자동화 실행 안에서는 첫 항목에서만 현재 상태를 읽고 image→video 전환/검증을 수행
  - 한 번 video 검증이 끝나면 이후 항목들은 다시 전환 검사를 반복하지 않고 바로 에셋 검색/프롬프트 입력으로 진행
- S자동화 전환 실패 시 즉시 중단:
  - 기존에는 S자동화 시작 전 image→video 전환이 실패해도 경고 로그만 남기고 그대로 에셋 검색/프롬프트 입력으로 계속 진행할 수 있었음
  - 이제는 전환 실패 또는 전환 후 재감지 결과가 video가 아니면 즉시 RuntimeError로 중단하고, image 상태로 계속 진행하지 않게 변경
- S자동화 시작 전 video 우선 판단 복구:
  - 사용자가 image 상태에서 S자동화를 시작하면, 기존에는 로컬 config의 `asset_prompt_media_mode` 값이 `image`로 저장된 경우 `생성 모드 유지: image`로 그대로 진행하는 문제가 있었음
  - 이제 S자동화 preset 적용은 저장값보다 현재 감지 상태를 먼저 보고, image면 반드시 video로 전환한 뒤 에셋 검색/프롬프트 입력으로 넘어감. 이미 video면 그대로 유지
- 프롬프트/S자동화 주기적 새로고침 추가:
  - 2026-03-30 기준, 프롬프트 자동화와 S자동화에는 `주기적 새로고침 사용` 설정을 추가
  - `몇 개마다 새로고침`과 `대기 3~5초 랜덤`을 설정할 수 있고, 성공 처리 후 지정 횟수마다 `F5`에 해당하는 페이지 새로고침과 안정화 대기를 수행
  - 사용자가 명확히 요청한 대로 다운로드 자동화는 제외. 다운로드는 업스케일 진행 상태가 끊길 수 있어 이 기능을 절대 타지 않음
- 프롬프트 개별 실행 체크 최종 반영:
  - 2026-03-30 기준, 프롬프트 자동화 `개별 실행 사용` 체크가 꺼져 있으면 번호를 입력해 둔 상태라도 실행 요약과 선택 입력은 `전체 프롬프트 실행`으로 처리
  - 즉 숫자를 메모처럼 적어두고 나중에 체크만 켜서 개별 실행하는 흐름이 이제 시작 시점 요약/리포트에도 그대로 반영됨
- 프롬프트 개별 실행 체크 추가:
  - 2026-03-30 기준, 프롬프트 자동화 `개별 실행` 옆에 `사용` 체크를 추가해 번호를 적어둔 채 보관할 수 있게 조정
  - 체크 ON이면 개별 실행, 체크 OFF이면 입력값은 남겨두되 전체 실행으로 동작
- 프롬프트 개별 실행 개수 미리보기 보강:
  - 2026-03-30 기준, 프롬프트 자동화 `개별 실행` 번호를 입력하면 이제 시작 전에도 상단 진행상황/총개수가 파일 전체 개수 대신 실제 선택 개수로 바로 반영되게 보강
  - 예: `1,2,3,28,53` 입력 시 상단도 135개가 아니라 5개 기준으로 즉시 바뀌도록 정리
- 번호 최대치 999 고정:
  - 2026-03-30 기준, S자동화/다운로드 자동화 번호는 이제 `001~999`까지만 허용되게 고정
  - 시작/끝 스핀박스는 999를 넘기지 않고, 개별 번호 직접 입력도 내부 파싱 단계에서 999 초과를 자동으로 막도록 정리
- 다운로드 번호칸 입력 꼬임 보정:
  - 2026-03-30 기준, 다운로드 자동화의 시작/끝 번호 칸을 편집할 때 내부 `S범위 다시 그리기`가 동시에 들어와 `00011000000`처럼 숫자가 불어나고 백스페이스가 잘 안 먹던 문제를 수정
  - 이제 다운로드 번호칸을 직접 편집 중일 때는 범위 재포맷을 건너뛰고, S번호 자릿수도 최대 4자리까지만 유지되게 보정
- 프롬프트 번호표 포함 입력 복원:
  - 2026-03-30 기준, 프롬프트 자동화에서 `S001 Prompt : ...` 형식은 번호 인식은 유지하면서도, `S001 Prompt :` 앞부분까지 실제 생성 프롬프트에 그대로 포함되게 다시 조정
- 프롬프트 파일 실제 번호 인식 보강:
  - 2026-03-30 기준, 프롬프트 자동화 메모장 파일은 이제 `S028 Prompt : ...` 형식도 실제 28번으로 읽도록 보강
  - 그래서 `1,2,3,28,53`처럼 개별 번호 실행할 때 파일 안의 4번째, 5번째 블록으로 밀리지 않고 적힌 실제 번호 기준으로 정확히 찾아가게 조정
- S개별 프롬프트 fallback 보강:
  - 2026-03-29 기준, S자동화 `개별 프롬프트 파일 사용`에서 파일에 없는 번호는 더 이상 무조건 누락 처리하지 않고, 작업창 아래 `프롬프트 템플릿` 칸의 공통 프롬프트로 자동 fallback 되게 조정
  - 그래서 파일에는 `S028::...`, `S053::...`처럼 정말 개별로 바꿀 번호만 적어두고, 나머지는 템플릿 칸의 공통 프롬프트를 그대로 쓰는 방식이 가능
- UI 정리 추가:
  - 2026-03-29 기준, `S번호 자동 반복 사용`과 `다운로드 번호 사용`은 이제 서로 하나만 체크되게 묶었고, S시작/다운로드시작 버튼을 눌러 실행할 때도 현재 번호 기준 체크 상태가 같이 따라가도록 정리
- UI 정리 작업:
  - 2026-03-29 기준, S자동화 `개별 프롬프트 파일 사용`은 이제 프롬프트 자동화 슬롯과 분리된 전용 파일 선택칸을 사용하도록 UI를 분리
  - S자동화 `프롬프트 템플릿` 입력칸은 1줄 Entry 대신 3줄 Text로 바꿔 긴 문장도 한 번에 더 잘 보이게 조정
  - 다운로드 자동화는 기능은 그대로 유지하고, 번호 범위/개별 번호 입력 UI를 별도 `다운로드 대상 번호 설정` 박스로 분리해 S자동화와 구분이 더 잘 되게 정리
- 프롬프트 inline 레퍼런스 재보정:
  - 2026-03-25 기준, `@ -> 오래된 순 -> S번호 입력` 흐름에서 정렬 적용 직후 에셋 검색 입력칸이 잠깐 다시 그려질 때 generic `input`을 잘못 집어 상단 다른 입력칸에 `S999`를 입력하는 문제가 발생
  - 원인은 `레퍼런스 검색창 재확인` 단계가 너무 느슨해서, 에셋 검색칸이 아닌 상단 입력칸도 후보로 허용하던 점
  - 현재는 레퍼런스 검색창을 상단 오버레이 입력칸 형태로 더 엄격하게 검사하고, `오래된 순` 클릭 뒤 기존 검색칸이 살아 있으면 그 locator를 그대로 재사용하도록 보강
  - 추가 원인 확인 결과, 기존 검색 locator가 죽었을 때도 `fill/type`가 기본 타임아웃까지 오래 매달리면서 1분 이상 지연된 뒤에야 `DOM 직접입력`으로 떨어지는 문제가 있었음
  - 현재는 프롬프트 자동화의 레퍼런스 검색 입력은 짧은 timeout으로만 재시도하고, 실패 시에도 일반 asset DOM 탐색이 아니라 상단 오버레이 에셋 검색칸 전용 DOM 입력으로 빠르게 폴백하도록 분리
  - 같은 날 추가 보강: 정렬 버튼에 이미 `오래된 순`이 표시된 상태면 메뉴를 다시 열지 않고, 그대로 현재 에셋 검색칸에 `S번호`를 입력하도록 분기
- 새 세션 이어가기 기준 정리:
  - 사용자는 Codex를 껐다 켜도 지금 맥락이 끊기지 않기를 매우 중요하게 생각함
  - 새 Codex를 열 때는 프로젝트 루트의 `새 Codex 시작용 한방 프롬프트.txt`를 그대로 붙여넣어 시작하는 방식을 기준으로 삼음
  - 다음 Codex는 이 파일을 기준으로 `AGENTS.md -> codex 설명서.md -> CODEX_연속작업_인수인계_20260304.md` 순서로 읽고 시작하면 됨
- 개인 설정 분리:
  - `flow/flow_config.json`은 이제 GitHub 업로드/다운로드 대상에서 제외하도록 정리 중이며, 데스크탑/노트북 개인 환경값이 서로 섞이지 않게 하는 기준 파일로 취급
- 프롬프트 파일 실제 번호 읽기 1차:
  - 프롬프트 파일 항목이 `S011` 또는 `S069::...` 형태로 시작하면 이제 순번 1,2,3이 아니라 실제 번호 11,69로 읽도록 파서/점프/개별 실행 경로를 1차 보강
  - 태그가 없는 예전 프롬프트 파일은 그대로 1,2,3 순서로 유지
- 다운로드 자동화 탐지 보정:
  - Flow 다운로드 화면이 `상단 S번호 + 큰 영상 타일` 구조일 때 카드 selector가 태그를 못 읽어서 `...` 버튼을 엉뚱한 곳에 누르던 문제를 수정
  - 왼쪽 영상 필터는 이제 아이콘-only 버튼도 위치/메타 점수로 더 안전하게 찾고, 카드 태그가 안 보여도 페이지에 `S001` 같은 번호가 보이면 대표 영상 타일 기준으로 더보기 버튼을 다시 찾게 보강
  - 추가로, 검색창에 입력된 `S002` 값만 보고 없는 번호를 존재로 오판해 맨 앞 영상을 다운받던 문제를 수정. 이제 실제 결과 제목/헤더/카드 같은 화면 텍스트에서 S번호가 확인될 때만 타일 fallback을 허용하고, 없으면 `검색 결과에 S002 항목이 없습니다.` 형태로 실패 처리
  - 이후 원인 추가 확인 결과, 내부 태그 판정에 숫자 단독 `2`, `002`도 포함돼 `x2`, `2부`, `1080P` 같은 화면 숫자와 충돌하던 점을 발견. 다운로드 태그 판정은 이제 `S002`, `S2` 같은 prefix 포함 형태만 인정
  - 현재는 검색 결과가 1~2개 타일로 보이면 대표 타일 기준으로 더보기 버튼을 시도하고, 없는 번호는 다운로드 대기 60초를 다 쓰지 않고 약 12초 안쪽에서 `검색 결과 없음`으로 빠르게 실패 처리
  - 연속 다운로드 중에는 `업스케일링이 완료되었습니다 / 닫기` 같은 알림 토스트가 카드 후보로 잘못 잡혀 이후 번호들이 전부 `검색 결과 없음`으로 실패하던 문제도 수정. 이제 알림/토스트는 카드 후보에서 제외하고 대표 타일까지 계속 탐색해서, 실사용 기준 S050 전후까지 연속 성공 확인
  - 2026-03-20 추가 보강: 1080P/4K 다운로드 시작 대기는 이제 브라우저 download 이벤트만 길게 기다리지 않고, 우측 상단 `업스케일링 중` / `업스케일링 완료` 토스트도 함께 감시함. `업스케일링 완료`가 뜨면 남은 120초를 끝까지 채우지 않고, 품질별 짧은 추가 유예(예: 1080P 12초 / 4K 20초)만 더 기다린 뒤 다음 단계로 진행하도록 조정
  - 같은 날 추가 보강: 이어달리기/일반 다운로드 공통으로 검색 결과가 완전히 비어 있으면 약 6초 안쪽에서 더 빠르게 `검색 결과 없음`으로 실패 처리하도록 조정했고, `720P`는 업스케일 토스트 감시 경로를 타지 않고 기존 `expect_download` 기반 즉시 다운로드 경로로 분리
- 아이콘 통일 작업:
  - 새 `icon.ico`와 `folder_icon.ico`를 같은 색감/문양으로 새로 생성해서 앱 창/작업표시줄/바탕화면 바로가기/폴더 아이콘이 한 세트처럼 보이게 정리
  - 폴더 `desktop.ini`도 `folder_icon.ico`를 보도록 바꾸고, 폴더 읽기전용 + `desktop.ini` 시스템/숨김 속성까지 적용
- 이어달리기 범위/성공 집계 보정:
  - `프롬프트 -> S반복 -> 다운로드` 단계 전환 때 이전 단계의 진행 위치(`index`)가 남아 S100 한 개만 돌고 성공으로 보이던 문제를 수정
  - 이어달리기 단계가 시작될 때마다 진행 위치를 0으로 초기화하고, `연속 범위` 단계에서는 숨은 `개별 번호` 값이 끼어들지 않게 보정
  - 계획한 개수보다 적게 실행되면 완료 요약/세션 리포트/다운로드 리포트에도 `실행되지 않음`으로 잡히게 보강
- 폴더 위치 이동 준비:
  - 실사용 폴더를 `C:\Users\jaekw\Flow Classic Plus`로 복사 완료
  - 바탕화면 `Flow Classic Plus.lnk` 바로가기도 새 위치 기준으로 다시 생성
  - 원래 `문서\Flow Classic Plus` 폴더는 현재 대화 세션이 잡고 있어 자동 삭제는 못 했고, 사용자가 새 위치 실행 확인 후 직접 지우면 됨
- 버전 표기 갱신:
  - 프로그램 상단 표시 버전을 `2026-03-18 Ver.01`로 변경
- 리포트 실패 번호 복붙 줄 추가:
  - 다운로드 리포트 / 세션 리포트 / 완료 요약에 실패 번호만 따로 모은 `S014,S017...` 형태의 `실패 번호 복붙` 한 줄 추가
  - 사용자가 JSON 리포트에서 실패 번호만 바로 복사해 `개별 번호` 칸에 붙여넣기 쉽게 정리
- 이어달리기 실패 자동 재시도 번호 형식 보정:
  - 실패 번호 자동 재시도 준비 시 `S014,S017`처럼 S가 붙은 실패 태그가 개별 번호칸에 그대로 들어가 경고가 뜨던 문제 수정
  - 이제 S/숫자 형식을 둘 다 허용하고, 자동 재시도 단계 생성 때는 `014,017`처럼 숫자형식으로 다시 넣어 경고 없이 진행
- Flow 자체 생성 실패 관찰:
  - 2026-03-17 현재 사용자는 Flow에서 Nano Banana Pro 이미지 생성, 영상 생성이 모두 실패하는 현상을 체감
  - 자동화 로직 고장으로 확정된 상태는 아니고, 우선 브라우저 쪽 최근 로그/캐시 정리 후 재부팅으로 1차 점검 예정
  - 다음 Codex는 브라우저 정리 후에도 같은 현상이 남는지부터 먼저 확인할 것
- 브라우저 프로필 복구 보강:
  - `브라우저 실행 1차 실패`, `브라우저 실행 2차 실패, 임시 프로필 폴백 시도` 로그가 반복될 때를 대비해 기본 설정 카드에 `새 브라우저 프로필 만들기` 버튼 추가
  - 버튼을 누르면 `browser_profile_dir`를 새 이름(`..._v2`, `..._v3`)으로 자동 바꾸고 새 폴더를 만든 뒤, 다음 `봇 작업창 열기`에서 그 새 프로필로 바로 로그인할 수 있게 정리
  - 현재 프로필 이름도 버튼 옆에서 바로 보이게 표시
- 다운로드 시작 대기시간 분리:
  - `실패 판단 대기(더보기 버튼 미탐지)`는 그대로 유지
  - 실제 파일 다운로드 시작 대기는 `품질별 자동 / 직접 입력`으로 분리
  - 자동값은 영상 기준 `720P 10초 / 1080P 60초 / 4K 180초`
- 이어달리기 실패 자동 재시도 1회 추가:
  - 이어달리기창 하단에 `실패 번호 자동 재시도 1회` 체크 추가
  - 이어달리기 1차 실행이 끝나면 실패한 `S자동화 / 다운로드 자동화` 번호만 뽑아 개별 번호 모드로 한 번 더 자동 실행
  - 프롬프트 자동화는 자동 재시도 대상에서 제외
- 이어달리기 실패 요약 보강:
  - 완료 팝업/완료 요약에 단계별 `실패 항목(S번호)`과 `실패 상세`를 같이 표시하도록 보강
  - `pipeline_report_*.json` 저장 시 `datetime` 직렬화 오류(`Object of type datetime is not JSON serializable`)가 나지 않도록 수정
- 이어달리기 완료 메타데이터 보강:
  - 마지막 단계(예: 다운로드) 1개만 보이던 완료 팝업/요약을 수정
  - 이제 `프롬프트 자동화 / S반복 자동화 / 다운로드 자동화` 각 단계 결과를 모두 모아서 마지막에 한 번에 표시
  - `pipeline_report_*.json` 파일도 따로 저장
- 원터치 실행 설정 창의 `프롬프트 슬롯` 아래에는 선택한 파일의 `총 개수 / 1~끝 범위`를 바로 표시해, 다시 작업창으로 돌아가 확인하지 않아도 되게 보강
- 원터치 실행 작은 창은 기본 크기를 키우고 리사이즈 가능하게 조정해, 상태 문구와 아래 버튼이 잘리지 않도록 보강
- 이어달리기 프리셋/원터치 기능 추가:
  - `작업 단계 목록` 아래에 `프리셋 저장` UI 추가
  - 현재 작업 단계 세트를 이름 있는 프리셋으로 저장 / 불러오기 / 이름변경 / 삭제 가능
  - 메인창과 이어달리기창에서 열 수 있는 작은 `원터치 실행` 창 추가
  - 원터치 START에서는 `프리셋 / 프롬프트 슬롯 / 다운로드 폴더 / 번호 방식(연속 범위 / 개별 번호)`만 따로 받고, 저장된 작업 1번부터 끝까지 기존 이어달리기 엔진으로 바로 실행
- 이어달리기창 UI 개편 진행:
  - 위에서부터 `이어달리기 설계 화면 -> 작업 단계 목록 -> 작업 상세 설정` 순서로 재배치
  - 작업 추가는 `프롬프트 자동화 / S반복 자동화 / 다운로드 자동화`를 처음부터 골라 넣는 방식으로 변경
  - 작업 상세 설정의 `작업 종류`는 추가한 종류를 그대로 보여주고 수정은 막음
  - 번호 입력은 `연속 범위 / 개별 번호` 라디오를 먼저 고르고, 선택하지 않은 입력칸은 비활성화
  - `다운로드 모드 / 품질 / 저장 폴더`는 다운로드 자동화일 때만 보이게 숨김 처리
- 프롬프트 자동화: 실제 입력/제출 성공
- S자동화: 실제 에셋 탐색/입력 성공
- 다운로드 자동화: 실제 다운로드 성공
- 생성 옵션은 프롬프트용과 S용을 분리함
- `현재 감지 생성 기본값` 라벨이 실제로 바뀌는 것 확인
- 노트북에서 실패하던 panel 내부 media 탭 탐색도 보강 후 성공 확인
- S자동화도 이제 공통 템플릿 대신 번호별 `개별 프롬프트 파일` 사용 가능
- S자동화 개별 프롬프트 파일은 `S004::프롬프트` 같은 태그형 지정과 태그 없는 공통 fallback을 같이 지원
- 프롬프트 자동화도 이제 프롬프트 안의 `@999` / `@S999` inline 레퍼런스 첨부 지원
- inline 레퍼런스 최종 해결: 본체 실행과 `레퍼런스 첨부 TEST` 모두 `@ 호출 -> S번호 입력 -> Enter`만 사용하고, `Enter 후 첫 결과 재클릭` 폴백을 제거한 뒤 다중 레퍼런스(`@S999,@S998`)도 사용자 실사용 기준 연속 성공 확인
- 프롬프트 자동화와 S반복 자동화 전용 휴식 설정 추가: `몇 개 작업 후`, `몇 분 휴식`을 직접 입력 가능
- 실제 휴식은 설정값 그대로가 아니라 ±30% 랜덤으로 흔들리게 적용하며, 다운로드 자동화는 휴식 없이 계속 진행
- inline 레퍼런스 첨부는 현재 실제 `@` 호출 방식으로 다시 전환, `Shift+2` 키 이벤트로 검색창을 연 뒤 `S999` 검색과 첫 결과 클릭으로 붙임
- `@` 호출은 현재 `page Shift+2 -> locator Shift+2 -> js dispatch` 3단 재시도 구조이며, 상단 레퍼런스 검색창도 일반 검색창과 별도로 전용 점수 로직으로 찾음
- `@` 호출 성공 판정은 이제 상단 검색창이 보인다는 이유만으로 통과하지 않고, 프롬프트 입력창에 실제 `@`가 들어갔는지도 같이 확인함
- 여러 명 inline 레퍼런스 첨부 뒤에는 입력창을 다시 클릭하지 않고 같은 커서 위치에서 바로 이어서 입력하도록 수정, 커서 이동 오동작을 줄임
- `@999,@998`처럼 레퍼런스가 연달아 붙는 경우는 다음 레퍼런스 직전에도 입력창 끝 커서 정렬을 한 번 더 수행
- 레퍼런스 첨부는 현재 `검색어 입력 -> Enter`를 기본으로 하고, Enter 뒤에도 검색창이 남을 때만 첫 결과 클릭 폴백을 수행
- 레퍼런스 첨부 후에는 입력창 재탐색 대신 기존 프롬프트 입력창을 그대로 유지하는 방향으로 단순화
- 레퍼런스 첨부 대기시간은 사용자가 체감하던 30초+ 수준에서 훨씬 짧게 줄이도록 조정
- 프롬프트 자동화 하단에서는 `레퍼런스 첨부 자동찾기` 버튼을 제거하고 `레퍼런스 첨부 TEST`만 남김
- 환경 모드 라벨은 시작 직후에도 저장된 현재 값 그대로 보이게 초기 문구 수정
- inline 레퍼런스 첨부는 `@` 키 이벤트 대신 `+ 버튼 -> 검색어 입력 -> 첫 결과 카드 클릭` 흐름으로 다시 보강
- 프롬프트 자동화 하단에 `레퍼런스 첨부 자동찾기 / TEST` 버튼 추가, 테스트 번호(`S999` 등)로 `+ / 검색 / 첫 결과`를 따로 점검 가능
- 레퍼런스 첨부 후 입력창 복귀는 이제 기존 프롬프트 입력창 근처를 우선해 다시 찾도록 바꿔, 상단 검색창으로 튀는 오탐을 줄임
- 시작 전 자동 점검은 이제 맨 앞에서 현재 이미지/동영상 상태를 먼저 감지하고, 그 상태를 기준으로 반대 방향 전환부터 검사한 뒤 원래 상태로 복귀함
- 프로그램 `A- / A+` 배율은 다시 자유 조절로 두고, 브라우저 쪽은 `노트북 모드 / 데스크탑 모드`가 각각 `작업창 크기 / 브라우저 배율` 값을 기억하도록 변경함
- 예전 `예약 시작`, `문서 이어달리기` UI는 숨기고, 이어달리기는 별도 이어달리기 창만 쓰는 방향으로 정리함
- 사용자가 수동으로 전부 정상 확인했고 자동 점검은 오탐이 있어 버튼/강제 게이트를 제거함
- 프롬프트/S자동화의 `생성 옵션 자동 맞춤`은 이제 체크박스 없이 항상 ON 고정이며, config에 꺼진 값이 있어도 시작 시 자동으로 True로 바로잡음

## 3. 현재 가장 중요한 고정 규칙
1. 원본 클래식은 보존
2. 개선 작업은 `Flow Classic Plus`에서만 진행
3. 기능은 클래식 기반 유지
4. 너무 큰 구조 변경 금지
5. 이어달리기는 맨 마지막 작업

## 4. 생성 옵션 관련 현재 기준
### 프롬프트 자동화
- 기본 기준 상태: 이미지
- `모드`는 이번 실행 목표(Image / Video)
- `기본값 이미지 맞춤 완료`는 출발 상태 확인 버튼

### S자동화
- 기본 기준 상태: 동영상
- S자동화는 영상 작업 기준
- `기본값 동영상 맞춤 완료` 확인 후 시작

### 저장/탐색 구조
- 프롬프트용 생성 옵션과 S용 생성 옵션을 분리
- 자동찾기/테스트 로그도 분리 저장

## 5. 최근 중요 커밋
- `4608f72` inline 레퍼런스를 `+ 버튼`으로 열도록 변경
- `b725581` media 탭 탐색 일반화와 패널 dump 보강
- `738a97a` 노트북에서 성공한 media 탭 탐색/패널 후보 dump 보강 원본 패치
- `76e3bdd` 입력방식 저장 fallback 보강, 번호 이동 칸 가독성 개선
- `4f0cb8f` S자동화 기본 기준 문구를 동영상 기준으로 정리
- `db52427` preset 로그 저장, radix id 저장 방식 보정
- `6068426` 봇 작업창 열기 버튼 추가
- `34038d1` 모델 드롭다운을 생성 옵션 패널 탐색 대상에서 제외

## 6. 다음 작업 우선순위
1. 메인 화면 카드 설명 글씨 잘림 등 디자인/배치 마감
2. 메인창 / 작업창 / 이어달리기창 UI 정리
3. 잘 쓰지 않는 기능과 고급 설정을 설정창으로 분리
4. 필요 없는 안내 요소(예: 우측 하단 봇 출동 카운트다운) 정리 여부 검토

## 7. 사용자가 최근에 원한 것
- 번호 이동 입력칸 가독성 개선
- 입력방식은 프로그램을 껐다 켜도 유지되게
- S 시작번호는 `001`처럼 보이고 저장도 유지되게
- 다운로드 실패 대기시간 직접 설정 가능하게
- UI는 나중에 크게 정리하되, 기능이 먼저
- 상단 `A- / % / A+` 확대축소는 하단 우측으로 이동
- 하단 `새로고침`, `파일 열기`는 `슬롯 동기화` 왼쪽/근처에 어울리게 재배치
- 상단 진행 상황 %는 중앙에 두고 막대형 그래픽으로 더 잘 보이게 변경
- 상단 `현재 상태`는 예전 확대/축소가 있던 자리로 이동
- 기능은 절대 건드리지 말고 디자인만 손보기

## 8. Git 운영 원칙
- `flow/flow_config.json`은 사용자 설정이므로 커밋 제외
- 다른 PC에서 `git pull` 또는 최신 동기화를 할 때도 `flow/flow_config.json`은 GitHub 버전으로 덮어쓰지 말고, 현재 PC 로컬 설정을 유지
- 커밋은 기능 단위로 짧고 명확하게
- 수정 후 가능한 즉시 push
- 앞으로는 작업할 때마다 `CODEX_연속작업_인수인계_20260304.md`와 `codex 설명서.md`도 같이 갱신

## 9. 문서 운영 규칙
- `CODEX_연속작업_인수인계_20260304.md` 파일명은 매일 바꾸지 않고 계속 유지
- 대신 `최신 업데이트` 시간과 내용만 계속 갱신
- 매 작업마다 설명서, 인수인계, 필요한 안내 문서까지 같이 업데이트
- 노트북 다운로드/실행 순서가 바뀌면 README도 같이 수정
## 10. 방금 전 작업 기록
- 2026-03-18 새 실행 위치/버전 갱신:
- 파일: `flow/flow_auto_v2.py`, `FlowClassicPlus_DesktopShortcut.bat`
- 새 실사용 위치는 `C:\Users\jaekw\Flow Classic Plus` 기준으로 사용하고, 바탕화면 바로가기를 새 위치 기준으로 다시 생성
- 프로그램 상단 버전 문자열을 `2026-03-18 Ver.01`로 갱신
- 2026-03-17 리포트 실패 번호 복붙 줄 추가:
- 파일: `flow/flow_auto_v2.py`
- 다운로드 리포트와 세션 리포트 JSON에 `failed_tags_compact` 필드를 추가하고, 완료 요약/팝업에도 `실패 번호 복붙` 줄을 표시
- 2026-03-17 실패 번호 자동 재시도 형식 수정:
- 파일: `flow/flow_auto_v2.py`
- `S014,S017`처럼 S가 붙은 개별 번호도 S자동화/다운로드 번호 입력에서 자동으로 숫자로 해석하게 수정
- 이어달리기 실패 자동 재시도 단계는 개별 번호칸에 `014,017` 같은 숫자형식으로 다시 저장되게 정리
- 2026-03-14 이어달리기창 UI 정리:
- 파일: `flow/flow_auto_v2.py`
- 작업 추가 버튼을 종류별로 분리하고, 번호 방식 라디오 및 다운로드 전용 상세 항목 숨김을 추가
- 2026-03-13 inline 레퍼런스 + 버튼 보강:
- 파일: `flow/flow_auto_v2.py`
- 실제 Flow에서는 봇의 `@` 문자 입력으로 검색창이 안 뜰 수 있어서, inline 레퍼런스는 이제 입력창 왼쪽 아래 `+` 버튼 클릭으로 에셋 검색창을 열도록 수정
- 2026-03-13 프롬프트 inline 레퍼런스 첨부 기능 추가:
- 파일: `flow/flow_auto_v2.py`
- 프롬프트 자동화는 이제 프롬프트 문자열 안의 `@999` 또는 `@S999`를 찾으면 그 자리에서 `@` 입력으로 에셋 검색창을 띄우고, `S999`를 입력한 뒤 다시 다음 문장을 이어서 입력
- 즉 별도 장면 번호 매핑 UI가 아니라, 프롬프트 파일 안에 직접 inline 토큰을 쓰는 구조
- 2026-03-12 S자동화 태그형 프롬프트 추가:
- 파일: `flow/flow_auto_v2.py`
- 선택한 프롬프트 파일 안에서 `S004::프롬프트`처럼 쓰면 해당 번호에만 개별 프롬프트 적용
- 태그 없는 프롬프트는 공통 fallback으로 사용
- 태그가 하나도 없는 기존 파일은 예전처럼 `1번=S001, 2번=S002` 순차 방식 그대로 유지
- 2026-03-12 S자동화 개별 프롬프트 파일 추가:
- 파일: `flow/flow_auto_v2.py`
- S자동화에 `개별 프롬프트 파일 사용` 체크와 프롬프트 슬롯 선택 콤보 추가
- 체크 시 `S001=1번 프롬프트`, `S002=2번 프롬프트` 방식으로 번호별 개별 프롬프트를 사용
- 프롬프트가 없는 번호는 경고 로그를 남기고 자동으로 건너뜀
- 2026-03-12 문서 최신화:
- 파일: `codex 설명서.md`, `CODEX_연속작업_인수인계_20260304.md`, `PROJECT_STATE.md`, `README.md`
- 현재 기준을 `데스크탑/노트북 모두 핵심 기능 통과 + 2026-03-12 완성품` 상태로 다시 정리
- 최신 `main` 커밋 `b725581`과 `complete-2026-03-12` 태그 기준을 문서에 반영
- 다음 큰 작업은 기능 개발보다 디자인/배치 마감이라는 점을 문서에 명시
- 파일: `flow/flow_auto_v2.py`
- 상단 헤더에서 확대/축소 버튼을 제거하고, 현재 상태 영역을 우측 상단 핵심 위치로 이동 중
- 하단 파일 선택 줄에서 `새로고침`, `파일 열기`를 `슬롯 동기화` 옆으로 옮기고, 우측 끝에는 확대/축소 버튼을 배치 중
- 상단 진행 상황은 숫자만 보이던 형태에서 중앙 막대형 시각 표시를 추가하도록 반영 중
- 사용자가 요청한 작업 방식도 문서에 기록: 매 작업마다 GitHub 업로드, 설명서 기록, 인수인계 문서 갱신
- 추가 조정: 우측 상단 `메인 메뉴` 버튼이 `현재 상태`보다 더 오른쪽 끝에 오도록 순서 재배치
- 파일: `flow/human_behavior_v2.py`
- 인간 페르소나 휴식 조건을 고정 느낌이 덜 나도록 `30~50개 작업 후 랜덤 휴식`으로 상향 조정
- 휴식 시간도 `5분~20분` 랜덤으로 늘려서 더 사람처럼 쉬도록 조정
- 파일: `flow/flow_auto_v2.py`
- UI 전체 배율(A-/A+) 범위를 `50%~150%`로 확대
- `봇 작업창 크기`, `브라우저 배율` 콤보도 `50%~150%`로 확장하고 실제 창 크기 계산 최소값도 낮춤
- 오른쪽 패널 마우스휠 스크롤이 안 먹던 문제를 공통 스크롤 라우팅으로 수정
- 창 가로폭이 줄어도 오른쪽 패널 최소폭과 스크롤바가 유지되도록 분할/반응형 보강
- 좁은 화면에서는 `프롬프트 자동화 시작`, `S반복 자동화 시작` 같은 오른쪽 실행 버튼 글자와 스타일을 더 작게 바꾸도록 조정
- 현재 버전은 사용자가 노트북에서 내려받아 시험할 `2026년 3월 10일 완성품` 기준점으로 보관
- README에 노트북 다운로드 후 실행 순서를 추가
- 인수인계 파일명은 계속 고정 유지하고, 내부 날짜/내용만 갱신하는 규칙을 문서에 명시
- `바탕화면_바로가기_생성.bat`를 `Flow Classic Plus` 전용 바로가기 생성기로 정리
- `FlowClassicPlus_DesktopShortcut.bat` 추가: 바탕화면에 `Flow Classic Plus.lnk` 생성
- 2026-03-11 설계 메모:
- `프롬프트 자동화 개별 작업`은 기존 프롬프트 파일 수동 편집으로 이미 가능하다고 정리
- 새로 필요한 것은 `S반복 자동화`와 `다운로드 자동화`용 개별 번호 작업
- 두 기능은 같은 번호 목록을 쓰므로 공용 `개별 번호 입력칸` 하나를 추가하는 방향이 가장 좋음
- 입력 형식 우선안: `005,018,048,057,071-079,110`
- 시작/끝 번호는 그대로 두고, 개별 입력칸이 비어 있지 않으면 그 목록을 우선 사용
- 직접 번호 클릭 체크 방식보다 빠르고, 실패 항목 재작업에 더 적합하다고 판단
- `이어달리기`는 이 개별 작업 자동화 구현이 끝난 뒤 설계 시작
- 2026-03-11 구현 반영:
- 파일: `flow/flow_auto_v2.py`
- 프롬프트 자동화용 `개별 실행` 입력칸 추가: 예시 `1,8,12-14,29`
- S반복 자동화용 `개별 번호` 입력칸 추가: 예시 `005,018,048,057,071-079,110`
- 다운로드 자동화는 위 S반복 개별 번호 입력칸을 그대로 공유해서 같은 번호만 다운로드 가능
- 입력 형식은 공백 무시, 중복 제거, 범위(`-`) 처리 지원
- S 시작/끝 번호 표시를 `001` 형식으로 기본 표시
- 완료 팝업 메시지를 더 자세히 보여주고, 동일한 텍스트를 `logs/completion_summary_*.txt`에 저장하도록 보강
- 세션/다운로드 리포트에 실행 대상 요약, 입력 원본, 재시도 오류 기록 저장 추가
- 2026-03-11 추가 구현:
- 파일: `flow/flow_auto_v2.py`
- 프롬프트 자동화와 S반복 자동화에 `최근 실패 자동채움` 버튼 연결 완료
- 최근 `session_report_*.json`, `download_report_*.json`에서 실패 번호를 읽어 개별 입력칸을 자동 채움
- 다운로드 저장 파일명은 `S001`, `S002` 같은 태그 이름으로 저장하고, 확장자만 원본 다운로드 형식대로 유지
- 2026-03-11 1차 창 분리:
- 파일: `flow/flow_auto_v2.py`
- 기존 홈 오버레이 방식 대신 별도 `메인창(Toplevel)`을 생성
- 기존 전체 기능은 `작업창(root)`에 그대로 유지
- 시작 시 메인창만 먼저 보이고, 큰 카드로 작업창을 여는 흐름으로 변경
- `메인 메뉴` 버튼을 누르면 작업창을 숨기고 메인창으로 돌아가는 구조로 변경
- 목적은 기능 변경 없이 화면 복잡도만 줄이는 것
- 2026-03-11 2차 창 분리:
- 파일: `flow/flow_auto_v2.py`
- 메인창에 `이어달리기` 큰 카드를 추가
- `이어달리기 전용 창(Toplevel)`을 별도로 생성해서 메인창에서 직접 열 수 있게 변경
- 이번 단계는 껍데기 분리만 완료했고, 실제 이어달리기 단계 저장/실행 기능은 다음 단계에서 붙일 예정
- 2026-03-11 이어달리기창 확장:
- 파일: `flow/flow_auto_v2.py`
- 이어달리기창 안에 `프로젝트 목록` 관리 영역 추가
- 프로젝트 목록 이름, URL, 프로젝트 이름을 저장 가능
- 추가 / 이름변경 / 삭제 / 저장 기능 연결 완료
- 아직 이 프로젝트 목록을 작업 단계와 연결하는 실행 기능은 붙이지 않았고, 저장 UI만 먼저 완성
- 2026-03-11 이어달리기 작업 단계 UI:
- 파일: `flow/flow_auto_v2.py`
- 프로젝트 목록 이름과 프로젝트 이름을 하나로 합쳐 `프로젝트 이름`만 쓰도록 단순화
- 이어달리기창 왼쪽 단계 목록에서 작업 추가 / 복제 / 삭제 / 위아래 이동 가능
- 오른쪽 상세 설정에서 작업 이름, 작업 종류, 프로젝트, 번호 범위, 개별 번호, 간격, 모드, 품질, 저장 폴더를 저장 가능
- 아직 실제 `이어달리기 시작` 실행 엔진은 붙이지 않았고, 저장 구조/UI까지만 완성
- 2026-03-11 이어달리기 시작 1차 연결:
- 파일: `flow/flow_auto_v2.py`
- 선택한 이어달리기 작업을 기존 작업창 설정값으로 그대로 적용하는 로직 추가
- `작업창 적용` 버튼 추가
- `이어달리기 시작` 버튼이 저장된 단계들을 순서대로 기존 `프롬프트/S반복/다운로드 시작` 함수에 연결하도록 1차 구현
- 한 단계가 끝나면 다음 단계로 자동 이동하고, 시작 자체가 실패하면 이어달리기 상태를 자동 해제하도록 안전장치 추가
- 2026-03-12 이어달리기 상세 설정 보강:
- 파일: `flow/flow_auto_v2.py`
- `프롬프트 자동화` 단계에 작업창 프롬프트 슬롯과 연결된 `프롬프트 파일` 선택 콤보 추가
- 선택한 프롬프트 슬롯의 실제 파일명, 총 프롬프트 개수, `1~끝번호` 범위를 작업 상세 설정에서 바로 표시
- `다운로드 자동화` 단계의 `품질`을 입력창이 아닌 선택 콤보로 변경
- `저장 폴더`는 직접 타이핑 대신 `폴더선택` 버튼으로만 고르도록 변경
- `프롬프트 자동화=이미지`, `S반복 자동화=동영상` 기본값 모드를 고정하고 이어달리기 시작 시에도 그대로 적용하도록 강제
- 2026-03-12 이어달리기 상세 설정 미세조정:
- 파일: `flow/flow_auto_v2.py`
- `다운로드 모드`, `품질`, `폴더선택`은 잠그지 않고 항상 사용자가 다시 바꿀 수 있게 수정
- 폴더선택 창은 현재 저장 경로가 없거나 잘못된 경로여도 실제 존재하는 폴더를 시작점으로 열리게 보정
- 2026-03-12 이어달리기 레이아웃 / 자동 전환 보강:
- 파일: `flow/flow_auto_v2.py`
- 이어달리기창을 세로 스크롤 구조로 바꾸고, `작업 단계 목록`을 화면 상단 카드로 이동
- 이어달리기창 크기를 더 작게 줄이고, 작업창 헤더에서 이어달리기창으로 바로 이동하는 버튼 추가
- 이어달리기창 상단에 `봇작업창 열기` 버튼 추가
- 생성 옵션 전환 로직은 현재 보이는 패널 버튼을 직접 읽어서 `이미지 ↔ 동영상` 전환 후 다시 닫도록 강화
- 프롬프트/S자동화 시작 전에 수동 기본값 확인 버튼을 강제하지 않도록 바꿔서 이어달리기 자동 전환이 막히지 않게 조정
- 다운로드 단계 적용 시 저장 폴더를 작업창 변수와 config에 다시 저장하고 로그에도 남기도록 보강
- 2026-03-12 생성 옵션 자동찾기 / 다운로드 저장 보강 2차:
- 파일: `flow/flow_auto_v2.py`
- 생성 옵션 자동찾기는 이제 현재 기본값이 `이미지`이든 `동영상`이든 현재 패널 버튼을 먼저 읽어서 양쪽 selector를 저장
- 작업창 프롬프트 / S자동화 영역에 `🧪 이미지→동영상`, `🧪 동영상→이미지` 테스트 버튼 추가
- 이어달리기 다운로드는 현재 단계의 저장 폴더를 `pipeline_active_output_dir`로 따로 들고 있다가 실제 저장 시 최우선으로 사용
- 2026-03-12 입력 방식 / 배율 / 이어달리기 순서 보강:
- 파일: `flow/flow_auto_v2.py`
- 입력 방식 선택은 이제 `typing`만 쓰도록 UI와 실행 로직 모두 고정
- `프롬프트(이미지)` 다음 `S자동화(동영상)`으로 넘어갈 때, 에셋 탐색보다 먼저 생성 옵션을 `동영상`으로 바꾸도록 순서를 수정
- `봇 작업창 크기`, `브라우저 배율`은 콤보박스 대신 `- / % / +` 버튼으로 실시간 반영되게 변경
- 생성 옵션/에셋 selector 자동찾기와 테스트 전에 현재 창 크기, 브라우저 배율, 페이지 스크롤 위치를 다시 맞춰서 노트북 작은 화면이나 축소 상태에서도 탐색 성공률을 높이도록 보강
- 2026-03-12 노트북 panel 내부 media 탭 탐색 보강:
- 파일: `flow/flow_auto_v2.py`
- 2026-03-12 다운로드 검색창 자동탐색 보강:
- 파일: `flow/flow_auto_v2.py`
- 작은 브라우저 창에서 다운로드 검색창이 중앙보다 왼쪽/아래에 열리면 기존 위치 조건 때문에 `검색 입력칸을 찾지 못했습니다.`로 실패하던 문제 확인
- 다운로드 검색창 탐색 범위를 완화하고, 검색 아이콘 클릭 뒤 `:focus`된 입력칸과 DOM 직접입력 폴백까지 추가해서 자동찾기/실행이 더 버티도록 보강
- 2026-03-12 이미지↔동영상 전환 오인 클릭 보강:
- 파일: `flow/flow_auto_v2.py`
- 전환 후보 탐색에서 왼쪽 `이미지 보기 / 동영상 보기`, `카메라`, `Veo 드롭다운` 같은 다른 버튼이 상위 후보로 잡히는 로그 확인
- 저장된 media selector가 반대 상태(`Image`)로 남아 있거나 이미지/동영상 panel selector가 같은 값일 때 fallback이 헷갈리던 문제를 줄이기 위해 상태별 selector 필터, 동일 selector 무시, 클릭 후 실제 감지 상태 재검증까지 추가
- 2026-03-12 100% 고정 잠금 / 시작 전 자동 점검 게이트 추가:
- 파일: `flow/flow_auto_v2.py`, `flow/flow_config.json`
- 프로그램 배율, 봇 작업창 크기, 브라우저 배율을 `100% 고정 잠금`으로 강제하는 UI와 저장 설정 추가
- 사용자가 `시작`, `S자동화`, `다운로드`, `이어달리기 시작`을 누르면 이제 먼저 자동 점검을 실행하고, `기본 selector / 프롬프트 전환 / S 전환 / S selector / 이미지 다운로드 / 영상 다운로드`가 전부 합격해야만 실제 자동화가 시작됨
- 현재 설정 파일 기본값도 `ui_zoom_percent=100`, `scale_lock_enabled=true`, `browser_window_scale_percent=100`, `browser_zoom_percent=100`으로 맞춰 둠
- `image 패널` 클릭 직후 visible 후보(button / role=button / role=tab / div / span)의 text, innerText, aria-label, title, role, class, bounding box를 액션 로그에 덤프하도록 추가
- `video/image` 탭 탐색은 이제 `role=tab`, title/aria-label, div 기반 탭, input 근처/오프너 근처/전역 탐색, 패널 열림 후 재탐색까지 순서대로 시도
- 2026-03-12 봇 작업창 크기 / S 에셋 탐지 추가 보강:
- 파일: `flow/flow_auto_v2.py`
- 봇 작업창 크기는 화면 안전 여백을 더 크게 잡고, 실제 브라우저 창도 좌상단 고정 위치로 열리게 해서 아래 잘림 현상을 줄이도록 조정
- 2026-03-13 inline 레퍼런스 추가 조작 제거:
- 파일: `flow/flow_auto_v2.py`
- 레퍼런스 뒤 복귀 클릭과 `End`, JS selection, zero-width 앵커 정리까지 전부 제거
- 현재는 레퍼런스 첨부 뒤 아무 조작 없이 바로 다음 텍스트/다음 레퍼런스로 넘어가는 가장 단순한 실험 버전
- 2026-03-13 inline 레퍼런스 단순 클릭 복귀 실험:
- 파일: `flow/flow_auto_v2.py`
- 레퍼런스 뒤 selection/caret 안정화 로직이 계속 칩 선택 상태를 만들 가능성이 있어, 이번엔 그 보정을 빼고 `입력창 빈 곳 클릭 + End`만 수행하도록 단순화
- inline 레퍼런스 뒤 텍스트와 다음 레퍼런스는 그 상태에서 바로 이어서 입력하도록 변경
- 2026-03-13 레퍼런스 커서 키보드 보정 제거:
- 파일: `flow/flow_auto_v2.py`
- 레퍼런스 첨부 뒤 `End / ArrowRight` 키를 추가로 누르던 보정이 오히려 칩 선택 상태를 만들 가능성이 있어 제거
- 이제는 JS selection으로 만든 텍스트 커서만 유지하도록 단순화
- 2026-03-13 inline 레퍼런스 커서 앵커 추가:
- 파일: `flow/flow_auto_v2.py`
- 레퍼런스 칩 뒤 커서가 다시 칩 삭제 버튼 쪽으로 걸리는 문제를 줄이기 위해, contenteditable 끝에 보이지 않는 zero-width 앵커를 붙이고 그 뒤로 selection을 보내도록 보강
- 프롬프트 입력 완료 후 제출 직전에는 이 zero-width 앵커를 DOM에서 제거해 실제 제출 텍스트에는 남지 않도록 정리
- 2026-03-13 inline 레퍼런스 속도/캐럿 추가 보정:
- 파일: `flow/flow_auto_v2.py`
- 레퍼런스 칩 뒤에서는 contenteditable 끝에 빈 텍스트 노드를 보장하고, 그 위치로 selection을 강제해서 칩 자체가 다시 선택/삭제되는 현상을 줄이도록 보강
- inline 레퍼런스 뒤 본문은 `page.keyboard.type(...)` 기반 실제 키 입력으로 바꾸고, inline 레퍼런스 프롬프트는 검토/망설임 대기를 생략해서 체감 속도를 줄이도록 조정
- 2026-03-13 inline 레퍼런스 커서 꼬임 보정:
- 파일: `flow/flow_auto_v2.py`
- `@` 레퍼런스 호출 직전에 입력창을 마우스로 다시 클릭하던 동작이 긴 문장 중간에 커서를 꽂아 `high-t@ech` 같은 오입력을 만들고 있었음
- 지금은 레퍼런스 직전 입력창 재클릭을 제거하고, `focus + 커서 끝 정렬`만 사용하도록 보정
- inline 레퍼런스 뒤 본문은 사람형 오타/머뭇거림 없이 `직선 입력`으로 처리해 칩 주변 커서 흔들림을 줄이도록 보강
- selector 탐색 준비 단계에서는 매번 창 크기를 다시 흔들지 않고 배율과 스크롤만 정리하도록 변경
- S 에셋 탐지는 이제 `시작`, `에셋 검색`, `검색 입력칸`을 페이지 상단만 보지 않고 조금씩 스크롤하며 재탐색하는 폴백을 추가
- 2026-03-12 S 에셋 자동찾기 실제 순서 보정:
- 파일: `flow/flow_auto_v2.py`
- 기존 자동찾기는 현재 보이는 화면에서만 `시작 / 에셋 검색 / 검색입력`을 찾고 있었고, `좌측 이미지 아이콘`을 먼저 여는 흐름이 빠져 있었음
- 지금은 자동찾기, 테스트, 실제 S반복 실행 모두에서 먼저 `좌측 이미지/에셋 아이콘 -> 시작 버튼 -> 에셋 검색창` 흐름을 열도록 보강
- 좌측 이미지 아이콘은 텍스트 selector만 의존하지 않고, 좌측 툴바 위치/크기/메타정보를 합쳐서 점수 기반으로 찾게 변경
- 추가 버그 수정:
- 자동찾기 로그에 `시작: text:시작`이 찍혀도, fallback으로 찾은 `시작` 버튼은 실제 클릭이 빠지던 문제가 있었음
- 지금은 fallback(`text:시작`, `text:Start`)으로 찾은 경우에도 같은 흐름으로 실제 클릭까지 수행
- 2026-03-12 이어달리기 S자동화 / 브라우저 탭 정리:
- 파일: `flow/flow_auto_v2.py`
- 이어달리기에서 `2번 작업=S자동화`로 넘어갈 때는 runner 시작 전에 먼저 `동영상 모드 맞춤`을 수행하도록 사전준비 로직 추가
- 즉 순서가 `이미지→동영상 전환 -> 좌측 이미지 아이콘 -> 시작 -> S검색 -> 프롬프트 입력` 흐름에 더 가깝게 맞춰짐
- 브라우저 세션 연결 시 persistent context에 남아 있던 여러 탭 중 시작 URL과 맞는 탭 1개만 남기고 나머지는 닫도록 정리
- 추가 보강:
- 테스트에서는 전환 성공인데 이어달리기만 실패하던 원인은, `이미지→동영상` 전환 시 입력칸 기준점 없이 전역 탐색으로만 버튼을 찾던 경로가 있었기 때문
- 지금은 이어달리기 사전준비와 실제 S실행 본체 모두에서 먼저 바닥 프롬프트 입력칸을 찾고, 그 입력칸 근처를 기준으로 생성 옵션 버튼을 찾게 수정
- 2026-03-12 현재 감지 상태 표시 추가:
- 파일: `flow/flow_auto_v2.py`
- 작업창 상단에 `현재 감지 생성 기본값` 라벨과 `상태 확인` 버튼 추가
- 봇 작업창 열기 직후, 생성 모드 전환 직후에도 같은 라벨이 자동으로 갱신되게 연결
- 사용자는 이제 봇이 읽은 현재 상태가 `이미지`인지 `동영상`인지 눈으로 바로 확인 가능
- 2026-03-12 자동 상태확인 / 기념판 기준 갱신:
- 파일: `flow/flow_auto_v2.py`, `README.md`, `PROJECT_STATE.md`
- 프롬프트 자동화와 S자동화는 시작할 때 맨 먼저 현재 감지 상태를 읽고 라벨/로그를 갱신
- 사용자가 깜빡하고 기본값을 안 맞춰놔도, 봇이 현재 상태를 먼저 읽고 그 다음 자동 전환 판단을 하도록 안정성 보강
- 현재 버전은 사용자가 실사용 성공 확인한 `2026년 3월 12일 완성품` 기준점으로 문서화
- 2026-03-12 노트북 성공 패치 병합:
- 파일: `flow/flow_auto_v2.py`
- 노트북에서 성공한 `media 탭 탐색 + 패널 후보 dump` 보강을 현재 데스크탑 작업본에도 반영
- `button`, `[role='button']`뿐 아니라 `[role='tab']`, `div/span 기반 탭`, `title`, `aria-label`, `data-testid`까지 함께 보도록 확장
- 실패 시에는 패널 내부 visible 후보 DOM 정보(text / innerText / aria / title / role / class / box / score)를 로그에 남겨 다음 수정 근거를 더 정확히 남기도록 보강
## 11. 다음 Codex가 시작할 때 할 일
1. `AGENTS.md`
2. `codex 설명서.md`
3. 이 문서
4. `PROJECT_STATE.md`
5. `git status --short`
6. 사용자에게 현재 상태를 짧게 설명 후 작업 시작

## 12. 2026-03-13 추가 메모
- 파일: `flow/flow_auto_v2.py`
- inline 레퍼런스 뒤 본문 입력은 초고속 별도 타이핑이 아니라 현재 `타이핑 속도` 설정을 따르도록 다시 맞춤
- `프롬프트 자동화 전용 생성 옵션`, `S자동화 전용 생성 옵션`의 자동 맞춤은 이제 체크 없이 항상 ON 고정
- 2026-03-14 inline 레퍼런스 정렬/보호모드 최종 보강:
- 파일: `flow/flow_auto_v2.py`
- inline 레퍼런스는 이제 `@` 호출 뒤 에셋창 오른쪽 정렬 버튼을 눌러 `오래된 순`을 먼저 선택한 다음, 검색창을 다시 잡아 `S999` 같은 태그를 입력하고 `Enter`로 붙이도록 수정
- 같은 태그가 여러 개 쌓여 있어도 최신 이미지가 아니라 가장 오래된 원본 레퍼런스를 먼저 고르는 흐름으로 정리
- 마지막 레퍼런스 뒤 본문은 처음 몇 글자만 보호 모드로 안전 입력하고, 그 뒤 남은 본문은 다시 `HumanActor.type_text()`로 넘겨 사람형 오타/멈칫/고민 입력을 복귀
- `레퍼런스 첨부 TEST`도 같은 본체를 타기 때문에 위 정렬 순서가 같이 적용됨
- 2026-03-31: 이미지 미니 워커 배치를 `기본 설정 / 번호 설정 / 요약 / 실행 버튼` 구조로 다시 정리하고, 워커별 배경색도 더 강하게 갈라서 작업표시줄 미리보기에서도 창 구분이 더 잘 되게 보강
- 2026-03-31: 프롬프트 자동화의 `개별 실행 사용` 체크를 제거. 이제 개별 번호칸에 값만 넣으면 바로 개별 실행으로 적용되고, 비워두면 전체 실행으로 단순화
- 2026-03-31: 이어달리기 마지막 자동 재시도는 이제 실패한 마지막 번호 목록(우선 다운로드 실패)을 기준으로 `프롬프트 -> S자동화 -> 다운로드` 전체 단계를 한 바퀴 더 다시 돌게 변경
- 2026-03-31: S자동화 워커도 이미지 워커처럼 미니 패널로 축소. 프로젝트/생성개수/작업간격/번호 방식/개별 프롬프트 파일/작업봇 열기/시작만 보이게 정리하고, 큰 본문은 숨기도록 전환
- 2026-03-31: 메인 카드의 `S001 자동화`도 이제 프롬프트 워커처럼 중간 런처 없이 실제 S자동화 미니 워커를 바로 열도록 변경
- 2026-04-01: 이어달리기 자동 재시도 1회를 모두 끝낸 뒤에도 실패 번호가 남아 있으면, 마지막으로 그 번호만 `다운로드 자동화 / 이미지 / 1K` 설정으로 한 번 더 정리 다운로드하도록 마무리 단계 추가
- 2026-04-01: 다운로드 워커도 프롬프트/S워커처럼 미니 패널로 축소하고, 메인 카드의 `다운로드 자동화`도 중간 런처 없이 실제 다운로드 미니 워커를 바로 열도록 변경
- 2026-04-03: 프롬프트 워커와 S워커는 이제 `생성 -> 대기 -> 다운로드`가 한 작업으로 묶여서 실행되고, 최종 성공 기준도 `생성 성공`이 아니라 `다운로드 실제 성공`으로 변경
- 2026-04-03: 프롬프트 워커는 이미지 다운로드, S워커는 영상 다운로드를 자동으로 이어서 수행하며, 다운로드 실패 시에는 해당 번호를 실패 처리하고 바로 다음 번호로 넘어가게 정리
- 2026-04-03: 워커 미니 UI 아래에 작업 대기열 패널을 추가해 각 번호가 `보류 중 / 실행 중 / 생성 대기 / 다운로드 중 / 완료 / 실패`로 바로 보이게 정리
- 2026-04-03: 메인 작업창의 `실시간 실패 번호` 패널과 내부 감시 코드는 제거. 이제 실패 추적은 실시간 번호 누적이 아니라 워커 대기열 상태와 완료 요약/세션 리포트 기준으로 확인
- 2026-04-03: 독립 `다운로드 자동화` 워커는 그대로 유지. 이번 병합은 프롬프트 워커와 S워커에만 적용
- 2026-04-03: 워커 HUD는 상단 헤더만 남기고, 중복되던 워커 요약 카드/HUD/설명 줄은 숨겨서 높이를 줄임. 상단 진행 카드에는 현재 프로젝트 이름도 같이 표시
- 2026-04-03: 프롬프트 워커 기본 설정 안에 `파일 열기 / 이름수정 / 삭제 / 추가 / 새로고침` 버튼과 파일 개수/번호 범위 요약을 넣어, 작업창으로 돌아가지 않고도 프롬프트 파일 관리 가능하게 보강
- 2026-04-03: 생성 대기 상태는 이제 대기열에서 `40초 -> 39초 ...` 식으로 실시간 감소 표시되며, 완료 팝업 뒤에도 대기열 성공/실패 목록은 유지되고 사용자가 `지우기` 버튼으로 직접 비울 수 있게 정리
- 2026-04-03: 워커 중지 시 브라우저 세션을 즉시 닫도록 정리했고, 워커 번호는 런처 카운터가 아니라 실제 실행 중인 워커 슬롯 파일 기준으로 잡아서 `1이 닫혔으면 다음 실행도 다시 1`이 되게 수정
- 2026-04-03: 워커 실행 브라우저 채널은 기본적으로 항상 `chrome`으로 강제하고, 이미지/S/다운로드 워커는 각각 다른 `.ico`를 읽도록 연결해 작업표시줄에서 구분되게 보강
- 2026-04-03: 워커 상단 `이미지 워커 / 미니 워커 창에서... / 워커 배지`와 중복 프로필 버튼 줄은 숨기고, 버튼은 각 워커 설정 카드 안으로 모아서 한 번만 보이게 정리
- 2026-04-03: 프로젝트 선택은 이제 워커 안에서 `프로젝트 추가 / 이름 변경 / URL 편집 / 삭제`가 가능하고, 프로젝트 콤보 더블클릭으로도 이름 변경 가능하게 보강
- 2026-04-03: 프롬프트/S워커는 `생성 개수`, `다운로드 전 대기`를 번호 설정 카드 쪽으로 옮겨 오른쪽 빈 공간을 줄이고 전체 높이를 더 압축
- 2026-04-03: 대기열 패널은 더 이상 1초마다 전부 다시 그리지 않고 같은 행 위젯만 갱신하도록 바꿔, 생성 대기 초가 내려갈 때 전체 카드가 깜빡이던 문제를 줄임
- 2026-04-03: 워커 `새로고침`은 기본 설정 카드 상단 1개만 남기고, 프롬프트 파일 줄의 중복 새로고침은 제거
- 2026-04-03: `새 프로필 / 이름 변경` 버튼은 기본 설정 카드에서 빼고, 하단 실행 줄의 `중지` 옆으로 이동. 프롬프트/S/다운로드 워커 모두 같은 위치 규칙으로 통일
- 2026-04-09: S자동화 개별 프롬프트 파일은 이제 `002>003 ||| 본문` 문법을 지원. `>`가 없으면 기존과 동일하게 시작 이미지 1장만 쓰고, `002>003`처럼 쓰면 시작 이미지 `S002`, 종료 이미지 `S003`를 함께 붙인 뒤 실제 프롬프트 입력은 `S002 Prompt : ...`로 유지
- 2026-04-10: 이미지 워커/S자동화 워커 하단 실행줄에 `⚙ 설정 접기/펼치기` 버튼 추가. 실행을 누르면 설정 카드가 자동으로 접혀 세로 공간을 대기열에 더 많이 넘기고, 다시 펼치면 기존 카드 구성이 그대로 돌아오도록 보강
- 2026-04-10: 이미지 워커/S자동화 워커에 `브라우저 실행 모드`를 추가해 `자동화 크롬` / `MS Edge 사람형`을 선택 가능하게 보강. `MS Edge 사람형`은 `msedge.exe --remote-debugging-port=... --user-data-dir=...` 방식으로 일반 Edge 창을 띄운 뒤 CDP로 연결하고, 실패 시에는 기존 자동화 크롬으로 안전하게 폴백
- 2026-04-10: 프롬프트/S자동화 공통 `주기적 새로고침`은 이제 고정 `N개마다`가 아니라 목표 횟수와 새로고침 전 대기시간을 각각 설정값 기준 ±30% 랜덤으로 새로 뽑아 쓰도록 변경. 실행 시작 때마다 다음 새로고침 목표도 새로 초기화되도록 정리
- 2026-04-10: `autostudio` 도구는 별도 저장소 `C:\Users\jaekw\autostudio` / GitHub `JaekwonJo/autostudio`로 분리 관리 중. 최근 안정화 포인트는 `파일 열기용 복사`(현재는 `"S001" "S002"` 같은 단순 오름차순 형식 유지), SRT 제작기 원클릭 `.srt` 저장, 방금 저장한 SRT 바로 자막 분할, 여러 `.txt/.srt` 파일 연속 장면 번호 분할
- 2026-04-17: `autostudio` 저장소에 `유튜브_쇼츠_API_발굴기.html` 추가. 공식 YouTube Data API v3만 사용하고, `search.list`로 짧은 영상 후보를 모은 뒤 `videos.list`로 조회수/길이/좋아요/댓글을 다시 읽어 `최소 조회수`, `최대 길이(초)`, `최근 며칠`, `페이지 수` 기준으로 고조회수 Shorts 후보를 거르는 구조. API 키와 검색 조건은 브라우저 localStorage에 저장됨. 공식 쿼터 기준도 화면에 같이 표시
- 2026-04-17: 이미지 워커 / S자동화 워커 하단 실행줄에서 `⚙ 설정 펼치기/접기`와 `▶ 생성+다운로드 시작` 버튼을 강조색 스타일로 분리. 접기 버튼은 주황, 시작 버튼은 초록으로 보여서 기본 어두운 버튼들 사이에서도 바로 찾게 정리
- 2026-04-17: `⚡자동화스튜디오.html` 파일관리 탭에 `S### -> @S###` 전용 `🏷️ @###으로 일괄 변경` 버튼 추가. 예를 들어 `S001.png, S002.png`처럼 이미 표준 이름인 파일도 한 번에 `@S001.png, @S002.png`로 바꿀 수 있고, 폴더 선택 직후 `일반 변경 대상 / @붙이기 대상` 개수 안내도 같이 보임
- 2026-04-17: `autostudio`의 유튜브 API 도구는 이제 화면 이름 `유튜브 핫토픽 레이더`를 쓰고, `쇼츠 발굴 / 오늘의 고조회수 영상 / 최근 N일 고조회수 영상` 3가지 검색 모드를 지원. 경제 / 역사 / 교육 / 과학 / AI / 시사 / 자기계발 프리셋 키워드를 미리 넣어 분야별 고조회수 영상을 빠르게 찾게 정리
- 2026-04-18: `유튜브 핫토픽 레이더`에 `쇼츠만 / 롱폼만` 길이 분류 필터, 결과 정렬 드롭다운(조회수/최신/좋아요/댓글/길이), API 키 영역 기본 접힘 추가. `전체 핫영상(트렌딩)` 모드도 `videos.list chart=mostPopular` 기반으로 실제 동작하게 연결
# 2026-04-18 작업 추가 메모
- `story_prompt_pipeline.py`와 `story_pipeline/` 모듈을 새로 추가해서, 사용자가 Gemini Gems에서 수작업으로 하던 `Step5 / Step6 / Step7` 자동화 흐름의 초안 구조를 만들었음.
- 현재 구조는 `문서 로딩 -> 배치 분할 -> Step5 생성 -> Step6 생성 -> Step7 검수 -> 코드 검수 -> 검증 통과분 즉시 저장` 순서임.
- 검증 통과 프롬프트는 `logs/story_prompt_pipeline/<worker_name>/<timestamp>_범위/validated_prompts_*.txt` 에 계속 누적 저장되게 해둠.
- `6_스토리_프롬프트_파이프라인_실행.bat` 는 기본 `story_worker1` 인스턴스용이고, `7_스토리_프롬프트_워커_추가실행.bat` 는 다른 워커 이름으로 추가 실행하는 용도임.
- 여러 워커를 동시에 돌릴 때는 반드시 워커 이름을 다르게 써야 하며, 그 이름 기준으로 설정 파일(`runtime/story_prompt_pipeline_config_<name>.json`)과 브라우저 프로필(`runtime/story_gemini_profile_pw_<name>`)이 분리됨.
- `manual_style`에서는 Step5를 건너뛰고, 실제 사용자 수작업처럼 `Step6 원문 + 장면 묶음`을 먼저 보내고 응답이 안정되면 같은 채팅에 `Step7 원문만` 보냄.
- Step7이 수정된 번호만 돌려주는 경우가 많아서, `PromptValidator.merge_partial_with_draft()`로 Step6 초안 전체에 Step7 수정본만 덮어써 최종 통합본을 만듦.
- 저장 파일은 통합본 `validated_prompts_*.txt` 외에 `validated_image_prompts_*.txt`, `validated_video_prompts_*.txt`도 같이 생성함.
- `story_pipeline/web.py`는 이제 입력후 대기 / 확인 간격 / 안정 횟수 / 최대 대기초를 설정값으로 받음.
- `story_pipeline/app.py`는 실행 시 `logs/story_prompt_pipeline/<worker>/ttz_worker_<worker>_<mode>_<stamp>.log` 로그 파일을 별도로 만들고, 로그 숨김/보기 토글과 현재 브라우저 프로필 표시를 제공함.
- Windows에서 `.bat`가 한글 VBS 파일명을 타고 들어가다 깨질 수 있어서, ASCII 이름의 `story_prompt_pipeline_run.vbs`, `story_prompt_pipeline_worker_run.vbs`를 새로 만들고 `.bat`는 이쪽을 호출하도록 바꿈.
- `브라우저` 버튼은 미리보기 전용 runner, `시작` 버튼은 자동화 전용 runner를 새 스레드에서 따로 생성하도록 분리했다. 그래서 브라우저를 먼저 열고 시작해도 greenlet 스레드 충돌이 줄어든다.
- worker 창 크기/위치(`window_geometry`)는 config에 자동 저장되어, 사용자가 늘려놓은 크기로 다음 실행 때 다시 뜬다.
- 아직 실제 Gemini Gems DOM에 맞춘 selector 미세보정과 실사용 1차 통과 테스트는 남아 있음. 다음 Codex는 `story_pipeline/web.py` 의 입력창/응답 selector 실측부터 보면 됨.
