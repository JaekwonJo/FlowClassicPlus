# CODEX 연속작업 인수인계 문서

- 문서명: `CODEX_연속작업_인수인계_20260304.md`
- 최신 업데이트: 2026-03-13 01:28 KST
- 목적: 다음 Codex가 지금 상태를 빠르게 이해하고 이어서 작업할 수 있게 하는 문서

## 1. 현재 안정 상태
- 원본 클래식은 건드리지 않고 보존
- 실제 개발판은 `Flow Classic Plus`
- 현재 기준 고정판: `2026년 3월 12일 완성품`
- GitHub `main` 최신 안정 커밋: `b725581`
- Git 태그 기준점: `complete-2026-03-12`
- 사용자 확인 완료 기능:
  - 프롬프트 자동화 정상
  - S001~S### 자동 반복 정상
  - 다운로드 자동화 정상
  - 프롬프트 생성 옵션 자동찾기/테스트 정상
  - S 생성 옵션 자동찾기/테스트 정상
  - 이어달리기 `프롬프트 -> S자동화 -> 다운로드` 정상
  - 데스크탑/노트북 모두 media 전환 테스트 정상

## 2. 최근 사용자 확인 완료 사항
- 프롬프트 자동화: 실제 입력/제출 성공
- S자동화: 실제 에셋 탐색/입력 성공
- 다운로드 자동화: 실제 다운로드 성공
- 생성 옵션은 프롬프트용과 S용을 분리함
- `현재 감지 생성 기본값` 라벨이 실제로 바뀌는 것 확인
- 노트북에서 실패하던 panel 내부 media 탭 탐색도 보강 후 성공 확인
- S자동화도 이제 공통 템플릿 대신 번호별 `개별 프롬프트 파일` 사용 가능
- S자동화 개별 프롬프트 파일은 `S004::프롬프트` 같은 태그형 지정과 태그 없는 공통 fallback을 같이 지원
- 프롬프트 자동화도 이제 프롬프트 안의 `@999` / `@S999` inline 레퍼런스 첨부 지원
- inline 레퍼런스 첨부는 `@` 키 이벤트 대신 `+ 버튼 -> 검색어 입력 -> 첫 결과 카드 클릭` 흐름으로 다시 보강
- 프롬프트 자동화 하단에 `레퍼런스 첨부 자동찾기 / TEST` 버튼 추가, 테스트 번호(`S999` 등)로 `+ / 검색 / 첫 결과`를 따로 점검 가능
- 레퍼런스 첨부 후 입력창 복귀는 이제 기존 프롬프트 입력창 근처를 우선해 다시 찾도록 바꿔, 상단 검색창으로 튀는 오탐을 줄임
- 시작 전 자동 점검은 이제 맨 앞에서 현재 이미지/동영상 상태를 먼저 감지하고, 그 상태를 기준으로 반대 방향 전환부터 검사한 뒤 원래 상태로 복귀함
- 프로그램 `A- / A+` 배율은 다시 자유 조절로 두고, 브라우저 쪽은 `노트북 모드 / 데스크탑 모드`가 각각 `작업창 크기 / 브라우저 배율` 값을 기억하도록 변경함
- 예전 `예약 시작`, `문서 이어달리기` UI는 숨기고, 이어달리기는 별도 이어달리기 창만 쓰는 방향으로 정리함
- 사용자가 수동으로 전부 정상 확인했고 자동 점검은 오탐이 있어 버튼/강제 게이트를 제거함

## 3. 현재 가장 중요한 고정 규칙
1. 원본 클래식은 보존
2. 개선 작업은 `Flow Classic Plus`에서만 진행
3. 기능은 클래식 기반 유지
4. 너무 큰 구조 변경 금지
5. 이어달리기는 맨 마지막 작업

## 4. 생성 옵션 관련 현재 기준
### 프롬프트 자동화
- 기본 기준 상태: 이미지
- `모드`는 이번 실행 목표(Image / Video)
- `기본값 이미지 맞춤 완료`는 출발 상태 확인 버튼

### S자동화
- 기본 기준 상태: 동영상
- S자동화는 영상 작업 기준
- `기본값 동영상 맞춤 완료` 확인 후 시작

### 저장/탐색 구조
- 프롬프트용 생성 옵션과 S용 생성 옵션을 분리
- 자동찾기/테스트 로그도 분리 저장

## 5. 최근 중요 커밋
- `4608f72` inline 레퍼런스를 `+ 버튼`으로 열도록 변경
- `b725581` media 탭 탐색 일반화와 패널 dump 보강
- `738a97a` 노트북에서 성공한 media 탭 탐색/패널 후보 dump 보강 원본 패치
- `76e3bdd` 입력방식 저장 fallback 보강, 번호 이동 칸 가독성 개선
- `4f0cb8f` S자동화 기본 기준 문구를 동영상 기준으로 정리
- `db52427` preset 로그 저장, radix id 저장 방식 보정
- `6068426` 봇 작업창 열기 버튼 추가
- `34038d1` 모델 드롭다운을 생성 옵션 패널 탐색 대상에서 제외

## 6. 다음 작업 우선순위
1. 메인 화면 카드 설명 글씨 잘림 등 디자인/배치 마감
2. 메인창 / 작업창 / 이어달리기창 UI 정리
3. 잘 쓰지 않는 기능과 고급 설정을 설정창으로 분리
4. 필요 없는 안내 요소(예: 우측 하단 봇 출동 카운트다운) 정리 여부 검토

## 7. 사용자가 최근에 원한 것
- 번호 이동 입력칸 가독성 개선
- 입력방식은 프로그램을 껐다 켜도 유지되게
- S 시작번호는 `001`처럼 보이고 저장도 유지되게
- 다운로드 실패 대기시간 직접 설정 가능하게
- UI는 나중에 크게 정리하되, 기능이 먼저
- 상단 `A- / % / A+` 확대축소는 하단 우측으로 이동
- 하단 `새로고침`, `파일 열기`는 `슬롯 동기화` 왼쪽/근처에 어울리게 재배치
- 상단 진행 상황 %는 중앙에 두고 막대형 그래픽으로 더 잘 보이게 변경
- 상단 `현재 상태`는 예전 확대/축소가 있던 자리로 이동
- 기능은 절대 건드리지 말고 디자인만 손보기

## 8. Git 운영 원칙
- `flow/flow_config.json`은 사용자 설정이므로 커밋 제외
- 커밋은 기능 단위로 짧고 명확하게
- 수정 후 가능한 즉시 push
- 앞으로는 작업할 때마다 `CODEX_연속작업_인수인계_20260304.md`와 `codex 설명서.md`도 같이 갱신

## 9. 문서 운영 규칙
- `CODEX_연속작업_인수인계_20260304.md` 파일명은 매일 바꾸지 않고 계속 유지
- 대신 `최신 업데이트` 시간과 내용만 계속 갱신
- 매 작업마다 설명서, 인수인계, 필요한 안내 문서까지 같이 업데이트
- 노트북 다운로드/실행 순서가 바뀌면 README도 같이 수정
## 10. 방금 전 작업 기록
- 2026-03-13 inline 레퍼런스 + 버튼 보강:
- 파일: `flow/flow_auto_v2.py`
- 실제 Flow에서는 봇의 `@` 문자 입력으로 검색창이 안 뜰 수 있어서, inline 레퍼런스는 이제 입력창 왼쪽 아래 `+` 버튼 클릭으로 에셋 검색창을 열도록 수정
- 2026-03-13 프롬프트 inline 레퍼런스 첨부 기능 추가:
- 파일: `flow/flow_auto_v2.py`
- 프롬프트 자동화는 이제 프롬프트 문자열 안의 `@999` 또는 `@S999`를 찾으면 그 자리에서 `@` 입력으로 에셋 검색창을 띄우고, `S999`를 입력한 뒤 다시 다음 문장을 이어서 입력
- 즉 별도 장면 번호 매핑 UI가 아니라, 프롬프트 파일 안에 직접 inline 토큰을 쓰는 구조
- 2026-03-12 S자동화 태그형 프롬프트 추가:
- 파일: `flow/flow_auto_v2.py`
- 선택한 프롬프트 파일 안에서 `S004::프롬프트`처럼 쓰면 해당 번호에만 개별 프롬프트 적용
- 태그 없는 프롬프트는 공통 fallback으로 사용
- 태그가 하나도 없는 기존 파일은 예전처럼 `1번=S001, 2번=S002` 순차 방식 그대로 유지
- 2026-03-12 S자동화 개별 프롬프트 파일 추가:
- 파일: `flow/flow_auto_v2.py`
- S자동화에 `개별 프롬프트 파일 사용` 체크와 프롬프트 슬롯 선택 콤보 추가
- 체크 시 `S001=1번 프롬프트`, `S002=2번 프롬프트` 방식으로 번호별 개별 프롬프트를 사용
- 프롬프트가 없는 번호는 경고 로그를 남기고 자동으로 건너뜀
- 2026-03-12 문서 최신화:
- 파일: `codex 설명서.md`, `CODEX_연속작업_인수인계_20260304.md`, `PROJECT_STATE.md`, `README.md`
- 현재 기준을 `데스크탑/노트북 모두 핵심 기능 통과 + 2026-03-12 완성품` 상태로 다시 정리
- 최신 `main` 커밋 `b725581`과 `complete-2026-03-12` 태그 기준을 문서에 반영
- 다음 큰 작업은 기능 개발보다 디자인/배치 마감이라는 점을 문서에 명시
- 파일: `flow/flow_auto_v2.py`
- 상단 헤더에서 확대/축소 버튼을 제거하고, 현재 상태 영역을 우측 상단 핵심 위치로 이동 중
- 하단 파일 선택 줄에서 `새로고침`, `파일 열기`를 `슬롯 동기화` 옆으로 옮기고, 우측 끝에는 확대/축소 버튼을 배치 중
- 상단 진행 상황은 숫자만 보이던 형태에서 중앙 막대형 시각 표시를 추가하도록 반영 중
- 사용자가 요청한 작업 방식도 문서에 기록: 매 작업마다 GitHub 업로드, 설명서 기록, 인수인계 문서 갱신
- 추가 조정: 우측 상단 `메인 메뉴` 버튼이 `현재 상태`보다 더 오른쪽 끝에 오도록 순서 재배치
- 파일: `flow/human_behavior_v2.py`
- 인간 페르소나 휴식 조건을 고정 느낌이 덜 나도록 `30~50개 작업 후 랜덤 휴식`으로 상향 조정
- 휴식 시간도 `5분~20분` 랜덤으로 늘려서 더 사람처럼 쉬도록 조정
- 파일: `flow/flow_auto_v2.py`
- UI 전체 배율(A-/A+) 범위를 `50%~150%`로 확대
- `봇 작업창 크기`, `브라우저 배율` 콤보도 `50%~150%`로 확장하고 실제 창 크기 계산 최소값도 낮춤
- 오른쪽 패널 마우스휠 스크롤이 안 먹던 문제를 공통 스크롤 라우팅으로 수정
- 창 가로폭이 줄어도 오른쪽 패널 최소폭과 스크롤바가 유지되도록 분할/반응형 보강
- 좁은 화면에서는 `프롬프트 자동화 시작`, `S반복 자동화 시작` 같은 오른쪽 실행 버튼 글자와 스타일을 더 작게 바꾸도록 조정
- 현재 버전은 사용자가 노트북에서 내려받아 시험할 `2026년 3월 10일 완성품` 기준점으로 보관
- README에 노트북 다운로드 후 실행 순서를 추가
- 인수인계 파일명은 계속 고정 유지하고, 내부 날짜/내용만 갱신하는 규칙을 문서에 명시
- `바탕화면_바로가기_생성.bat`를 `Flow Classic Plus` 전용 바로가기 생성기로 정리
- `FlowClassicPlus_DesktopShortcut.bat` 추가: 바탕화면에 `Flow Classic Plus.lnk` 생성
- 2026-03-11 설계 메모:
- `프롬프트 자동화 개별 작업`은 기존 프롬프트 파일 수동 편집으로 이미 가능하다고 정리
- 새로 필요한 것은 `S반복 자동화`와 `다운로드 자동화`용 개별 번호 작업
- 두 기능은 같은 번호 목록을 쓰므로 공용 `개별 번호 입력칸` 하나를 추가하는 방향이 가장 좋음
- 입력 형식 우선안: `005,018,048,057,071-079,110`
- 시작/끝 번호는 그대로 두고, 개별 입력칸이 비어 있지 않으면 그 목록을 우선 사용
- 직접 번호 클릭 체크 방식보다 빠르고, 실패 항목 재작업에 더 적합하다고 판단
- `이어달리기`는 이 개별 작업 자동화 구현이 끝난 뒤 설계 시작
- 2026-03-11 구현 반영:
- 파일: `flow/flow_auto_v2.py`
- 프롬프트 자동화용 `개별 실행` 입력칸 추가: 예시 `1,8,12-14,29`
- S반복 자동화용 `개별 번호` 입력칸 추가: 예시 `005,018,048,057,071-079,110`
- 다운로드 자동화는 위 S반복 개별 번호 입력칸을 그대로 공유해서 같은 번호만 다운로드 가능
- 입력 형식은 공백 무시, 중복 제거, 범위(`-`) 처리 지원
- S 시작/끝 번호 표시를 `001` 형식으로 기본 표시
- 완료 팝업 메시지를 더 자세히 보여주고, 동일한 텍스트를 `logs/completion_summary_*.txt`에 저장하도록 보강
- 세션/다운로드 리포트에 실행 대상 요약, 입력 원본, 재시도 오류 기록 저장 추가
- 2026-03-11 추가 구현:
- 파일: `flow/flow_auto_v2.py`
- 프롬프트 자동화와 S반복 자동화에 `최근 실패 자동채움` 버튼 연결 완료
- 최근 `session_report_*.json`, `download_report_*.json`에서 실패 번호를 읽어 개별 입력칸을 자동 채움
- 다운로드 저장 파일명은 `S001`, `S002` 같은 태그 이름으로 저장하고, 확장자만 원본 다운로드 형식대로 유지
- 2026-03-11 1차 창 분리:
- 파일: `flow/flow_auto_v2.py`
- 기존 홈 오버레이 방식 대신 별도 `메인창(Toplevel)`을 생성
- 기존 전체 기능은 `작업창(root)`에 그대로 유지
- 시작 시 메인창만 먼저 보이고, 큰 카드로 작업창을 여는 흐름으로 변경
- `메인 메뉴` 버튼을 누르면 작업창을 숨기고 메인창으로 돌아가는 구조로 변경
- 목적은 기능 변경 없이 화면 복잡도만 줄이는 것
- 2026-03-11 2차 창 분리:
- 파일: `flow/flow_auto_v2.py`
- 메인창에 `이어달리기` 큰 카드를 추가
- `이어달리기 전용 창(Toplevel)`을 별도로 생성해서 메인창에서 직접 열 수 있게 변경
- 이번 단계는 껍데기 분리만 완료했고, 실제 이어달리기 단계 저장/실행 기능은 다음 단계에서 붙일 예정
- 2026-03-11 이어달리기창 확장:
- 파일: `flow/flow_auto_v2.py`
- 이어달리기창 안에 `프로젝트 목록` 관리 영역 추가
- 프로젝트 목록 이름, URL, 프로젝트 이름을 저장 가능
- 추가 / 이름변경 / 삭제 / 저장 기능 연결 완료
- 아직 이 프로젝트 목록을 작업 단계와 연결하는 실행 기능은 붙이지 않았고, 저장 UI만 먼저 완성
- 2026-03-11 이어달리기 작업 단계 UI:
- 파일: `flow/flow_auto_v2.py`
- 프로젝트 목록 이름과 프로젝트 이름을 하나로 합쳐 `프로젝트 이름`만 쓰도록 단순화
- 이어달리기창 왼쪽 단계 목록에서 작업 추가 / 복제 / 삭제 / 위아래 이동 가능
- 오른쪽 상세 설정에서 작업 이름, 작업 종류, 프로젝트, 번호 범위, 개별 번호, 간격, 모드, 품질, 저장 폴더를 저장 가능
- 아직 실제 `이어달리기 시작` 실행 엔진은 붙이지 않았고, 저장 구조/UI까지만 완성
- 2026-03-11 이어달리기 시작 1차 연결:
- 파일: `flow/flow_auto_v2.py`
- 선택한 이어달리기 작업을 기존 작업창 설정값으로 그대로 적용하는 로직 추가
- `작업창 적용` 버튼 추가
- `이어달리기 시작` 버튼이 저장된 단계들을 순서대로 기존 `프롬프트/S반복/다운로드 시작` 함수에 연결하도록 1차 구현
- 한 단계가 끝나면 다음 단계로 자동 이동하고, 시작 자체가 실패하면 이어달리기 상태를 자동 해제하도록 안전장치 추가
- 2026-03-12 이어달리기 상세 설정 보강:
- 파일: `flow/flow_auto_v2.py`
- `프롬프트 자동화` 단계에 작업창 프롬프트 슬롯과 연결된 `프롬프트 파일` 선택 콤보 추가
- 선택한 프롬프트 슬롯의 실제 파일명, 총 프롬프트 개수, `1~끝번호` 범위를 작업 상세 설정에서 바로 표시
- `다운로드 자동화` 단계의 `품질`을 입력창이 아닌 선택 콤보로 변경
- `저장 폴더`는 직접 타이핑 대신 `폴더선택` 버튼으로만 고르도록 변경
- `프롬프트 자동화=이미지`, `S반복 자동화=동영상` 기본값 모드를 고정하고 이어달리기 시작 시에도 그대로 적용하도록 강제
- 2026-03-12 이어달리기 상세 설정 미세조정:
- 파일: `flow/flow_auto_v2.py`
- `다운로드 모드`, `품질`, `폴더선택`은 잠그지 않고 항상 사용자가 다시 바꿀 수 있게 수정
- 폴더선택 창은 현재 저장 경로가 없거나 잘못된 경로여도 실제 존재하는 폴더를 시작점으로 열리게 보정
- 2026-03-12 이어달리기 레이아웃 / 자동 전환 보강:
- 파일: `flow/flow_auto_v2.py`
- 이어달리기창을 세로 스크롤 구조로 바꾸고, `작업 단계 목록`을 화면 상단 카드로 이동
- 이어달리기창 크기를 더 작게 줄이고, 작업창 헤더에서 이어달리기창으로 바로 이동하는 버튼 추가
- 이어달리기창 상단에 `봇작업창 열기` 버튼 추가
- 생성 옵션 전환 로직은 현재 보이는 패널 버튼을 직접 읽어서 `이미지 ↔ 동영상` 전환 후 다시 닫도록 강화
- 프롬프트/S자동화 시작 전에 수동 기본값 확인 버튼을 강제하지 않도록 바꿔서 이어달리기 자동 전환이 막히지 않게 조정
- 다운로드 단계 적용 시 저장 폴더를 작업창 변수와 config에 다시 저장하고 로그에도 남기도록 보강
- 2026-03-12 생성 옵션 자동찾기 / 다운로드 저장 보강 2차:
- 파일: `flow/flow_auto_v2.py`
- 생성 옵션 자동찾기는 이제 현재 기본값이 `이미지`이든 `동영상`이든 현재 패널 버튼을 먼저 읽어서 양쪽 selector를 저장
- 작업창 프롬프트 / S자동화 영역에 `🧪 이미지→동영상`, `🧪 동영상→이미지` 테스트 버튼 추가
- 이어달리기 다운로드는 현재 단계의 저장 폴더를 `pipeline_active_output_dir`로 따로 들고 있다가 실제 저장 시 최우선으로 사용
- 2026-03-12 입력 방식 / 배율 / 이어달리기 순서 보강:
- 파일: `flow/flow_auto_v2.py`
- 입력 방식 선택은 이제 `typing`만 쓰도록 UI와 실행 로직 모두 고정
- `프롬프트(이미지)` 다음 `S자동화(동영상)`으로 넘어갈 때, 에셋 탐색보다 먼저 생성 옵션을 `동영상`으로 바꾸도록 순서를 수정
- `봇 작업창 크기`, `브라우저 배율`은 콤보박스 대신 `- / % / +` 버튼으로 실시간 반영되게 변경
- 생성 옵션/에셋 selector 자동찾기와 테스트 전에 현재 창 크기, 브라우저 배율, 페이지 스크롤 위치를 다시 맞춰서 노트북 작은 화면이나 축소 상태에서도 탐색 성공률을 높이도록 보강
- 2026-03-12 노트북 panel 내부 media 탭 탐색 보강:
- 파일: `flow/flow_auto_v2.py`
- 2026-03-12 다운로드 검색창 자동탐색 보강:
- 파일: `flow/flow_auto_v2.py`
- 작은 브라우저 창에서 다운로드 검색창이 중앙보다 왼쪽/아래에 열리면 기존 위치 조건 때문에 `검색 입력칸을 찾지 못했습니다.`로 실패하던 문제 확인
- 다운로드 검색창 탐색 범위를 완화하고, 검색 아이콘 클릭 뒤 `:focus`된 입력칸과 DOM 직접입력 폴백까지 추가해서 자동찾기/실행이 더 버티도록 보강
- 2026-03-12 이미지↔동영상 전환 오인 클릭 보강:
- 파일: `flow/flow_auto_v2.py`
- 전환 후보 탐색에서 왼쪽 `이미지 보기 / 동영상 보기`, `카메라`, `Veo 드롭다운` 같은 다른 버튼이 상위 후보로 잡히는 로그 확인
- 저장된 media selector가 반대 상태(`Image`)로 남아 있거나 이미지/동영상 panel selector가 같은 값일 때 fallback이 헷갈리던 문제를 줄이기 위해 상태별 selector 필터, 동일 selector 무시, 클릭 후 실제 감지 상태 재검증까지 추가
- 2026-03-12 100% 고정 잠금 / 시작 전 자동 점검 게이트 추가:
- 파일: `flow/flow_auto_v2.py`, `flow/flow_config.json`
- 프로그램 배율, 봇 작업창 크기, 브라우저 배율을 `100% 고정 잠금`으로 강제하는 UI와 저장 설정 추가
- 사용자가 `시작`, `S자동화`, `다운로드`, `이어달리기 시작`을 누르면 이제 먼저 자동 점검을 실행하고, `기본 selector / 프롬프트 전환 / S 전환 / S selector / 이미지 다운로드 / 영상 다운로드`가 전부 합격해야만 실제 자동화가 시작됨
- 현재 설정 파일 기본값도 `ui_zoom_percent=100`, `scale_lock_enabled=true`, `browser_window_scale_percent=100`, `browser_zoom_percent=100`으로 맞춰 둠
- `image 패널` 클릭 직후 visible 후보(button / role=button / role=tab / div / span)의 text, innerText, aria-label, title, role, class, bounding box를 액션 로그에 덤프하도록 추가
- `video/image` 탭 탐색은 이제 `role=tab`, title/aria-label, div 기반 탭, input 근처/오프너 근처/전역 탐색, 패널 열림 후 재탐색까지 순서대로 시도
- 2026-03-12 봇 작업창 크기 / S 에셋 탐지 추가 보강:
- 파일: `flow/flow_auto_v2.py`
- 봇 작업창 크기는 화면 안전 여백을 더 크게 잡고, 실제 브라우저 창도 좌상단 고정 위치로 열리게 해서 아래 잘림 현상을 줄이도록 조정
- selector 탐색 준비 단계에서는 매번 창 크기를 다시 흔들지 않고 배율과 스크롤만 정리하도록 변경
- S 에셋 탐지는 이제 `시작`, `에셋 검색`, `검색 입력칸`을 페이지 상단만 보지 않고 조금씩 스크롤하며 재탐색하는 폴백을 추가
- 2026-03-12 S 에셋 자동찾기 실제 순서 보정:
- 파일: `flow/flow_auto_v2.py`
- 기존 자동찾기는 현재 보이는 화면에서만 `시작 / 에셋 검색 / 검색입력`을 찾고 있었고, `좌측 이미지 아이콘`을 먼저 여는 흐름이 빠져 있었음
- 지금은 자동찾기, 테스트, 실제 S반복 실행 모두에서 먼저 `좌측 이미지/에셋 아이콘 -> 시작 버튼 -> 에셋 검색창` 흐름을 열도록 보강
- 좌측 이미지 아이콘은 텍스트 selector만 의존하지 않고, 좌측 툴바 위치/크기/메타정보를 합쳐서 점수 기반으로 찾게 변경
- 추가 버그 수정:
- 자동찾기 로그에 `시작: text:시작`이 찍혀도, fallback으로 찾은 `시작` 버튼은 실제 클릭이 빠지던 문제가 있었음
- 지금은 fallback(`text:시작`, `text:Start`)으로 찾은 경우에도 같은 흐름으로 실제 클릭까지 수행
- 2026-03-12 이어달리기 S자동화 / 브라우저 탭 정리:
- 파일: `flow/flow_auto_v2.py`
- 이어달리기에서 `2번 작업=S자동화`로 넘어갈 때는 runner 시작 전에 먼저 `동영상 모드 맞춤`을 수행하도록 사전준비 로직 추가
- 즉 순서가 `이미지→동영상 전환 -> 좌측 이미지 아이콘 -> 시작 -> S검색 -> 프롬프트 입력` 흐름에 더 가깝게 맞춰짐
- 브라우저 세션 연결 시 persistent context에 남아 있던 여러 탭 중 시작 URL과 맞는 탭 1개만 남기고 나머지는 닫도록 정리
- 추가 보강:
- 테스트에서는 전환 성공인데 이어달리기만 실패하던 원인은, `이미지→동영상` 전환 시 입력칸 기준점 없이 전역 탐색으로만 버튼을 찾던 경로가 있었기 때문
- 지금은 이어달리기 사전준비와 실제 S실행 본체 모두에서 먼저 바닥 프롬프트 입력칸을 찾고, 그 입력칸 근처를 기준으로 생성 옵션 버튼을 찾게 수정
- 2026-03-12 현재 감지 상태 표시 추가:
- 파일: `flow/flow_auto_v2.py`
- 작업창 상단에 `현재 감지 생성 기본값` 라벨과 `상태 확인` 버튼 추가
- 봇 작업창 열기 직후, 생성 모드 전환 직후에도 같은 라벨이 자동으로 갱신되게 연결
- 사용자는 이제 봇이 읽은 현재 상태가 `이미지`인지 `동영상`인지 눈으로 바로 확인 가능
- 2026-03-12 자동 상태확인 / 기념판 기준 갱신:
- 파일: `flow/flow_auto_v2.py`, `README.md`, `PROJECT_STATE.md`
- 프롬프트 자동화와 S자동화는 시작할 때 맨 먼저 현재 감지 상태를 읽고 라벨/로그를 갱신
- 사용자가 깜빡하고 기본값을 안 맞춰놔도, 봇이 현재 상태를 먼저 읽고 그 다음 자동 전환 판단을 하도록 안정성 보강
- 현재 버전은 사용자가 실사용 성공 확인한 `2026년 3월 12일 완성품` 기준점으로 문서화
- 2026-03-12 노트북 성공 패치 병합:
- 파일: `flow/flow_auto_v2.py`
- 노트북에서 성공한 `media 탭 탐색 + 패널 후보 dump` 보강을 현재 데스크탑 작업본에도 반영
- `button`, `[role='button']`뿐 아니라 `[role='tab']`, `div/span 기반 탭`, `title`, `aria-label`, `data-testid`까지 함께 보도록 확장
- 실패 시에는 패널 내부 visible 후보 DOM 정보(text / innerText / aria / title / role / class / box / score)를 로그에 남겨 다음 수정 근거를 더 정확히 남기도록 보강
## 11. 다음 Codex가 시작할 때 할 일
1. `AGENTS.md`
2. `codex 설명서.md`
3. 이 문서
4. `PROJECT_STATE.md`
5. `git status --short`
6. 사용자에게 현재 상태를 짧게 설명 후 작업 시작
